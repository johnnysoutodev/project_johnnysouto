# Plano de Migração — Site em Angular + i18n (pt-BR / es-ES / en-US)

> Documento vivo de planejamento. Ainda em fase de planejamento — nenhuma implementação começou. Complementa `docs/ANALISE-PROJETO.md` (estado atual do repositório) e `docs/ai-instructions.md` (regras gerais para IAs).

**Última atualização:** 06/09/2026
**Status:** 📋 Planejamento

---

## 1. Objetivo

Migrar o site pessoal/currículo de Johnny Souto (hoje HTML/CSS/JS estático + Materialize + Grunt) para uma aplicação **Angular**, com:

1. Design vindo de um arquivo **Figma**, incluindo **dark mode** e **menu mobile dedicado** (ambos já contemplados no design, ver `docs/design-system.md`).
2. Internacionalização completa: **pt-BR** (idioma padrão/inicial), **es-ES** e **en-US**.
3. **Zero quebra** do site em produção (`https://www.johnnysouto.com.br`) durante o processo.
4. **Google Analytics** continuando a rastrear normalmente após a migração.

## 2. Decisões arquiteturais já tomadas

Estas decisões foram discutidas e definidas antes de detalhar as fases — qualquer mudança nelas deve atualizar este documento.

| Decisão | Escolha | Por quê |
|---|---|---|
| Renderização | **Prerender estático (SSG)**, via `@angular/ssr`/Angular prerendering, sem servidor Node em produção | Mantém o deploy 100% estático na Vercel (`outputDirectory` de arquivos prontos, como hoje), preservando SEO, meta tags e Open Graph por página — sem precisar de runtime Node a cada request. |
| Internacionalização | **i18n nativo do Angular** (compile-time, um build por idioma) | Melhor performance e SEO por idioma (cada idioma vira HTML totalmente traduzido e prerenderizado), mesmo trade-off que hoje já existe manualmente (`src/pt/`, `src/en/`). Custo: trocar um texto exige rebuild — aceitável para um site de currículo, que muda com pouca frequência. |
| Estratégia de rollout | **Construir em paralelo, trocar no final** | O novo site Angular é desenvolvido isolado (branch e/ou projeto de preview próprio na Vercel), sem tocar no pipeline Grunt/`public/` atual. A produção só passa a apontar para o novo build depois de validado e aprovado. |
| Origem do design | **Figma fornecido pelo Johnny** | O Claude/Copilot extrai especificações (cores, espaçamentos, tipografia, componentes) via o servidor MCP do Figma já configurado em `.vscode/mcp.json`, em vez de desenhar o design do zero. |
| Dark mode | **Entra no escopo** | O Figma já contempla variantes Light/Dark completas (mesmas seções, tokens de cor próprios) — ver `docs/design-system.md`. Implementar como parte natural do design system, com alternância manual e respeito a `prefers-color-scheme` como padrão inicial. |
| Menu mobile | **Entra no escopo** | O Figma já contempla um menu mobile dedicado (overlay), tanto em Light quanto em Dark — ver `docs/design-system.md`. Não é uma funcionalidade nova a inventar, só a implementar a partir do que já está desenhado. |
| Breakpoint tablet | **Entra no escopo, sem referência no Figma** | O Figma só tem Desktop (1440) e Mobile/iPhone 8 (375) — nenhum frame de tablet (confirmado em `docs/design-system.md`, seção 7). Johnny decidiu que o site precisa se comportar bem em tablet mesmo assim. Convenção proposta (não extraída do Figma): tablet ~768–1023px, desktop a partir de 1024px — padrão comum de mercado (ex.: breakpoints `md`/`lg` do Tailwind CSS). O layout intermediário em si (como cada seção se comporta nessa faixa) é decisão de implementação da Fase 2, interpolando entre os paddings/gaps mobile e desktop já documentados, não uma cópia de spec do Figma. |

## 3. Escopo de conteúdo (paridade com o site atual)

Baseado no inventário de `docs/ANALISE-PROJETO.md`, tudo isto precisa ter equivalente no novo site antes do corte de produção:

- [ ] Página principal (CV/currículo) — hoje `src/index.html`.
- [ ] Versão pt-BR — hoje `src/pt/index.html` → passa a ser o idioma padrão (`/` ou `/pt-br/`, a definir na Fase 3).
- [ ] Versão en-US — hoje `src/en/index.html` → rota `/en-us/`.
- [ ] Versão **es-ES — nova**, não existe hoje.
- [ ] **CV para download — novo formato**, substituindo a versão de impressão em HTML (`src/printer/index.html`, que sai do escopo desta migração). Documento de currículo baixável nos três idiomas: o idioma do arquivo baixado acompanha o idioma atual do site (ex.: usuário navegando em `/en-us/` baixa o CV em inglês). Formato exato do arquivo e forma de geração/entrega ainda a decidir (ver Fase 4).
- [ ] Meta tags de SEO/Open Graph (title, description, keywords, `og:title`, `og:description`, `og:image`, `og:url`, `og:site_name`) — por idioma.
- [ ] `robots.txt`.
- [ ] Favicon e imagens (`bg.png`, `perfil_jjns.jpeg`).
- [ ] **Google Analytics** (`gtag.js`, ID atual `UG-YYR4SND80L`) — funcionando em todas as rotas/idiomas.
- [ ] `cleanUrls` / comportamento de URLs equivalente ao `vercel.json` atual.
- [ ] **Dark mode — novo**, não existe hoje. Alternância manual (toggle) com persistência da escolha do usuário, respeitando `prefers-color-scheme` como padrão inicial.
- [ ] **Menu mobile dedicado — novo**, não existe hoje (site atual não tem navegação mobile própria). Overlay conforme design do Figma, funcionando em ambos os temas.
- [ ] **Suporte a tablet — novo**, sem referência de layout no Figma (só Desktop/Mobile). Breakpoint proposto ~768–1023px (ver Decisões arquiteturais); layout intermediário a definir na Fase 2.

## 4. Estrutura de pastas proposta

Para não colidir com o que existe (e não repetir o problema do antigo diretório `app/`, que ficou solto e não rastreado), a app Angular nasce num diretório novo e claramente nomeado, convivendo com o site atual até o corte final:

```
project_johnnysouto/
├── src/            # site atual (legado) — intocado durante a migração
├── public/         # build atual (legado) — intocado durante a migração
├── angular-app/    # NOVO projeto Angular (nome confirmado, scaffold da Fase 1 já criado)
│   ├── src/
│   │   ├── app/
│   │   ├── locale/         # arquivos de tradução (pt-BR, es-ES, en-US)
│   │   └── ...
│   ├── angular.json
│   └── package.json
├── docs/
│   ├── ANALISE-PROJETO.md
│   ├── PLANO-MIGRACAO-ANGULAR.md   # este documento
│   ├── design-system.md           # specs extraídas do Figma via MCP
│   └── ...
```

> Depois do corte de produção (Fase 8), `src/` e `public/` (legado) e o `gruntfile.js` são removidos, e o conteúdo de `angular-app/` pode subir para a raiz, se fizer sentido na época.

## 5. Fases

### Fase 0 — Preparação
- [x] Johnny compartilha o arquivo/link do Figma com o design: <https://www.figma.com/design/9z2dzCKhlXWqVynN5SEeEM/template_portfolio_website?node-id=0-1&m=dev&t=p400RgMm8j8SjqJM-1>
- [x] Extrair specs do Figma via MCP (cores, tipografia, espaçamentos, componentes, breakpoints). Specs de nível página/tema salvas em `docs/design-system.md`; specs de componentes individuais ficam para a Fase 2, extraídas sob demanda.
- [x] Nome final do diretório do novo projeto confirmado: `angular-app/` (Johnny confirmou; scaffold da Fase 1 já criado com esse nome).
- [x] Versão do Angular definida: a mais recente estável no momento da Fase 1 (Angular 21/22), que exige **Node ~24.16.x** — diferente do Node 20 do `.nvmrc` atual (legado, usado pelo Grunt). O novo projeto Angular precisa de um pin de versão de Node próprio (ex.: `.nvmrc`/Volta dentro do diretório do projeto), sem alterar o `.nvmrc` da raiz enquanto o site legado ainda depender de Node 20.
- [ ] Decidir se a Vercel vai ter um **projeto separado** para o preview do novo site ou um **environment/branch** dentro do mesmo projeto.

### Fase 1 — Scaffold do projeto Angular

> Executado pelo agente `angular-scaffold` (`docs/agent-rules/angular-scaffold.md`), não por um `ng new` avulso — ele cuida de rodar o scaffold isolado do repositório existente, sem Git aninhado, com a versão de Node/Angular certa.

- [x] `ng new` do novo projeto dentro de `angular-app/`, com roteamento, SSR/prerender e **SCSS** habilitados desde o início, e `--skip-git` (não pode criar um repositório Git aninhado dentro deste repositório). Rodado via `npx @angular/cli@22.1.7 new angular-app --directory=angular-app --style=scss --routing --ssr --skip-git --ai-config=claude-code --ai-config=vscode --package-manager=npm --interactive=false`.
- [x] Pinado **Node 24.18.0** (a versão ativa no terminal no momento do scaffold, dentro do range exigido pelo Angular 22.1.7: `^22.22.3 || ^24.15.0 || >=26.0.0`) só para o novo projeto, via `angular-app/.nvmrc` e `engines.node` em `angular-app/package.json`, sem alterar o `.nvmrc` da raiz (Node 20, ainda usado pelo Grunt legado).
- [x] Configurada a integração de IA do Angular CLI para **Claude Code** (`--ai-config=claude-code`, gera `CLAUDE.md` + `.mcp.json`) **e VSCode/GitHub Copilot** (`--ai-config=vscode`, gera `AGENTS.md` + `.vscode/mcp.json`) no novo subprojeto. Nota: na versão 22.1.7 do Angular CLI, o schematic `ai-config` usado pelo `ng new` não tem mais uma opção dedicada "copilot" (só existe em uma versão mais antiga do schematic, usada por `ng generate ai-config` isoladamente) — a opção `vscode` (gera `AGENTS.md`, formato que o GitHub Copilot no VS Code também lê, + `.vscode/mcp.json`) é a equivalente mais próxima nesta versão do CLI, confirmada via `--help` antes de usar.
- [x] Configurado lint/format: ESLint via `ng add @angular-eslint/schematics@22.2.0` (`eslint.config.js`, builder `@angular-eslint/builder:lint`, script `npm run lint`) + Prettier (`.prettierrc`/`.editorconfig` já gerados pelo `ng new`, com scripts `npm run format`/`npm run format:check` adicionados). Todo o projeto gerado foi formatado uma vez (`npm run format`) para começar consistente.
- [x] Pipeline de CI mínimo criado em `.github/workflows/Angular-App-CI.yaml`: roda só quando algo dentro de `angular-app/**` muda (`paths` filter), instala com `npm ci`, roda lint e `ng build`. Não altera `Develop.yaml`/`Production.yaml`.

### Fase 2 — Design system a partir do Figma

> Geração dos componentes executada pelo agente `angular-components` (`docs/agent-rules/angular-components.md`), que consome o que já está em `docs/design-system.md` — não se conecta ao Figma nem cria o projeto Angular por conta própria.

- [x] Tokens de design (cores, tipografia, espaçamento) extraídos do Figma — light e dark (ver `docs/design-system.md`) — e migrados para dentro do projeto Angular como CSS custom properties (`angular-app/src/styles/_tokens.scss`), prontos para os dois temas via `[data-theme="dark"]` (toggle ainda não ligado).
- [x] Componentes reutilizáveis (header, seções do CV, footer), já preparados para os dois temas (ex.: via CSS variables/tokens, não cores fixas) — as 7 seções de conteúdo + Header/Footer/menu mobile, todas usando tokens (nunca cor fixa).
  - [x] Subcomponentes base: Icon Button (36×36/44×44, design-system.md 8.3) e Tag (8.4) — `angular-app/src/app/shared/components/`.
  - [x] Header (8.1) — `angular-app/src/app/layout/header/` (layout estático, sem sticky/scroll e sem menu mobile ainda).
  - [x] Footer (8.2) — `angular-app/src/app/layout/footer/`.
  - [x] Hero (8.8) — `angular-app/src/app/features/hero/`, conteúdo real (nome, cargo, bio-rascunho, GitHub/LinkedIn) e sem bloco Location+Hire (sem dado real). Demais seções seguem próximas fatias (plano detalhado em `.claude/plans/splendid-stirring-flute.md`: T1/T2 assets+decisões de conteúdo concluídas, T3 dark mode, T4 designer sweep, T5 página Home/rotas, T6 Hero concluída, T7-T12 uma seção por vez, T13 menu mobile, T14 QA final). **Atualizado (07/09/2026, a pedido do Johnny):** foto real (`profile_johnnysouto.jpeg`, `priority`) no lugar do bloco neutro + iniciais original — ver design-system.md seção 10.
  - [x] About (8.9) — `angular-app/src/app/features/about/`, `host: { id: 'about' }` (âncora `#about` do Header). Foto sempre à esquerda (sem alternância), bio-rascunho expandida a partir da do Hero, checklist "quick bits" (2×2) com 4 fatos reais e verificáveis (anos de experiência, formação Uninove, inglês intermediário, estado civil). **Atualizado (07-08/09/2026, a pedido do Johnny):** foto real (`photo_about_02.jpeg`) no lugar do bloco neutro original — por privacidade, é uma foto neutra (ipê-amarelo florido), não um retrato; o texto do estado civil também foi ajustado pra não citar o nome do filho ("Casado e pai de um menino").
  - [x] Skills (8.10) — `angular-app/src/app/features/skills/`, conteúdo real agrupado por categoria (Back-End, Banco de Dados, Front-End, Versionamento, Pessoais — src/pt/index.html), só 3 itens com ícone real (JS/Node/Git). Grid do Figma (flat, 2×8, sem categorias) adaptado para uma linha `flex-wrap` por categoria; achado do "item sem ícone desalinhado" (8.10) resolvido reservando um slot de ícone de altura fixa (64px) mesmo nos itens texto-only.
  - [x] Experience (8.6/8.11) — `angular-app/src/app/features/experience/`, entradas reais da "Experiência Profissional" de `src/pt/index.html`, sem conector visual de timeline (8.6, confirmado ausente). **Atualizado (08/09/2026, a pedido do Johnny):** (1) logo de empresa real (não mais texto) pra todas exceto RCS Sistemas (sem arquivo disponível); (2) Vivo (SOC + OSS) unificado num único card com 2 cargos empilhados (`ExperienceItem.positions`), 1 logo só, no lugar de 2 cards separados com o mesmo logo duplicado — ver design-system.md seção 10.
  - [x] Project (8.5, renomeado de "Work" em 08/09/2026 — o componente Angular chamava `Work`, mas o conteúdo real sempre foram projetos; o frame do Figma continua se chamando "Work") — `angular-app/src/app/features/project/`, `host: { id: 'project' }`, 2 projetos reais (Pitcher, ProFuturo — não 3, seção 10), logo real de cada projeto centralizado sobre o bloco neutro original (era um bloco vazio); ação do card omitida no Pitcher (sem link real) e apontando para o link real do ProFuturo já existente no legado (Fundação Telefônica Vivo).
  - [x] Testimonials (8.7) — `angular-app/src/app/features/testimonials/`, `host: { id: 'testimonials' }`, 3 depoimentos fictícios (seção 10) claramente marcados como placeholder (nomes "Depoimento de exemplo 1/2/3" + nota visível no subtítulo do heading).
  - [x] Contact me (8.12) — `angular-app/src/app/features/contact-me/`, `host: { id: 'contact' }`, conteúdo real (email, WhatsApp, GitHub, LinkedIn) empilhado numa única coluna central; ação de copiar (email/telefone) via Clipboard API real, guardada por `isPlatformBrowser`.
- [x] Menu mobile dedicado (overlay), conforme design do Figma, nos dois temas — `angular-app/src/app/layout/mobile-menu/` (design-system.md 8.13) + botão de hamburguer novo em `layout/header/`.
- [x] Toggle de dark mode (componente + lógica de alternância/persistência) — `angular-app/src/app/core/theme/theme.ts` (`ThemeService`, signal-based, `localStorage` + `prefers-color-scheme`), ligado ao botão do Header e do menu mobile; script anti-FOUC em `src/index.html`.
- [x] Aplicar o design às páginas, sem ainda ligar i18n (conteúdo fixo em pt-BR nesta fase) — `pages/home/` compõe as 7 seções + Header/Footer/menu mobile, registrada na rota `''`.
- [x] Definir e implementar o layout do breakpoint tablet (~768–1023px) para cada seção — sem referência no Figma, interpolando entre os paddings/gaps mobile e desktop já documentados em `docs/design-system.md` — mixins `_breakpoints.scss`/`_layout.scss` (`mobile-only`/`tablet-up`/`tablet-only`/`desktop-up`, `section-padding`) reutilizados em todas as seções e no Header/menu mobile.

### Fase 3 — Internacionalização
- [ ] Configurar i18n nativo do Angular com os três locales: `pt-BR` (padrão), `es-ES`, `en-US`.
- [ ] Extrair todo o texto para arquivos de tradução.
- [ ] Traduzir para `es-ES` (conteúdo novo) e revisar `en-US` (já existe uma versão manual hoje, usar como base).
- [ ] Definir estrutura de rotas por idioma (ex.: `/`, `/es-es/`, `/en-us/` — ou `/pt-br/` explícito; decidir junto com SEO/`hreflang`).
- [ ] Adicionar tags `hreflang` para SEO multilíngue.

### Fase 4 — Paridade de conteúdo e SEO
- [ ] Migrar todas as meta tags/Open Graph por idioma (ver checklist da seção 3).
- [ ] Recriar `robots.txt` e (se fizer sentido) adicionar `sitemap.xml` com as variantes de idioma.
- [ ] Implementar o download do CV nos três idiomas, entregando o arquivo no idioma atual do site — **decidir e documentar aqui** a forma de geração/padronização do download (ex.: PDF estático por idioma gerado em build, ou outra abordagem) antes de implementar.
- [ ] Validar favicon e imagens.

### Fase 5 — Google Analytics
- [ ] Reimplementar o carregamento do `gtag.js` no Angular (ex.: serviço de analytics carregado no bootstrap, respeitando o prerender/SSG).
- [ ] Validar que pageviews são disparados corretamente em navegação entre rotas Angular (Router events), já que isso não é automático como em HTML estático.
- [ ] Confirmar que o ID de measurement é o mesmo (`UG-YYR4SND80L`) ou decidir se troca.
- [ ] Testar em ambiente de preview antes do corte, validando no próprio Google Analytics que os hits chegam.

### Fase 6 — Build & deploy
- [ ] Configurar build de produção com prerender para os 3 idiomas.
- [ ] Novo `vercel.json` (ou config equivalente) apontando para a saída do build Angular.
- [ ] Deploy de **preview** (não produção) para validação.

### Fase 7 — QA e validação lado a lado
- [ ] Comparação visual página a página com o site atual.
- [ ] Lighthouse / Core Web Vitals no novo site vs. atual.
- [ ] Checklist de SEO (meta tags, `hreflang`, sitemap, robots).
- [ ] Confirmação de que o Google Analytics está recebendo dados do preview (idealmente numa property/stream de teste, para não misturar com dados reais de produção).
- [ ] Revisão de conteúdo com o Johnny (principalmente as traduções em `es-ES`).
- [ ] Validar dark mode em todas as páginas/idiomas (contraste, persistência da escolha, `prefers-color-scheme` inicial).
- [ ] Validar menu mobile dedicado em diferentes tamanhos de tela e nos dois temas.
- [ ] Validar layout em tablet (~768–1023px) em todas as seções e nos dois temas — sem referência do Figma, então a comparação aqui é contra o critério de "bom senso responsivo", não contra um design de origem.
- [ ] Validar que o download do CV entrega o arquivo no idioma correspondente em cada rota/idioma do site.

### Fase 8 — Corte de produção
- [ ] Definir plano de rollback (como voltar ao site atual rapidamente se algo der errado).
- [ ] Apontar o domínio de produção (`www.johnnysouto.com.br`) para o novo deploy.
- [ ] Monitorar Analytics, erros e Search Console nas primeiras 24–48h.

### Fase 9 — Pós-migração
- [ ] Remover `src/`, `public/`, `gruntfile.js` e dependências do Grunt (após confirmação de estabilidade).
- [ ] Atualizar `docs/ANALISE-PROJETO.md` para descrever a nova stack.
- [ ] Atualizar `docs/ai-instructions.md` (a regra atual diz para não introduzir framework reativo sem alinhamento prévio — este plano é esse alinhamento; a regra deve ser atualizada para refletir Angular como stack oficial).
- [ ] Arquivar ou marcar este documento como concluído no log de evolução.

## 6. Riscos e mitigação

| Risco | Mitigação |
|---|---|
| Perder posicionamento de SEO na troca | Prerender estático + `hreflang` + validação de meta tags antes do corte (Fase 7); manter mesma estrutura de URLs quando possível. |
| Google Analytics parar de registrar durante/depois da troca | Testar em property/stream de teste antes; validar pageviews via Router events; monitorar de perto nas primeiras 48h (Fase 8). |
| Traduções de `es-ES` com baixa qualidade | Revisão humana (Johnny ou revisor nativo) antes do corte — não depender só de tradução automática. |
| Regressão visual/funcional no site atual durante o desenvolvimento | Novo projeto isolado em `angular-app/`, sem tocar em `src/`/`public/`/workflows atuais até a Fase 8. |
| Escopo crescer demais (ex.: querer CMS, blog, etc. no meio do caminho) | Este plano cobre só migração de framework + i18n + design (incluindo dark mode e menu mobile, já previstos no Figma); qualquer funcionalidade nova além disso entra como item futuro, não nesta migração. |
| Dark mode gerar flash de tema errado (FOUC) no site prerenderizado (SSG) | Aplicar o tema antes do primeiro paint (inline script mínimo lendo preferência salva/`prefers-color-scheme`), validado na Fase 7. |

## 7. Critérios de sucesso (Definition of Done)

- Site novo em Angular, publicado em produção, com paridade total de conteúdo com o site atual.
- Três idiomas funcionando (`pt-BR`, `es-ES`, `en-US`), com troca de idioma acessível ao usuário.
- Dark mode funcionando em produção, com alternância manual e persistência da escolha.
- Menu mobile dedicado funcionando em produção, nos dois temas.
- Layout funcionando bem em tablet (~768–1023px), mesmo sem referência de origem no Figma.
- Google Analytics confirmado funcionando em produção após o corte.
- SEO (meta tags, Open Graph, `hreflang`, robots/sitemap) validado antes do corte.
- Nenhum período de indisponibilidade do site atual durante o desenvolvimento.
- `docs/ANALISE-PROJETO.md` e `docs/ai-instructions.md` atualizados refletindo a nova stack.

## 8. Log de evolução deste plano

| Data | Mudança |
|---|---|
| 06/09/2026 | Criação do plano, com decisões arquiteturais iniciais definidas (SSG, i18n nativo, rollout em paralelo, design via Figma/MCP). |
| 06/09/2026 | Conectado ao MCP do Figma e extraídas specs de página/tema (cores, tipografia, sombras, seções, breakpoints) em `docs/design-system.md`. Design contempla dark mode e menu mobile dedicado, ainda não previstos neste plano — decisão pendente. |
| 06/09/2026 | Dark mode e menu mobile dedicado adicionados ao escopo da migração (Johnny confirmou), já que ambos estão presentes no design do Figma. Atualizado objetivo, decisões arquiteturais, escopo de conteúdo, Fase 2, Fase 7, riscos e critérios de sucesso. |
| 06/09/2026 | Wrapper do novo projeto renomeado de `web/` para `angular-app/` (nome final ainda a confirmar), mantendo o isolamento do site legado. Versão do Angular definida como Angular 21/22, exigindo Node ~24.16.x — pin de versão próprio do novo projeto, sem alterar o `.nvmrc` (Node 20) da raiz, que o site legado ainda usa. |
| 06/09/2026 | Criado o agente `angular-scaffold` (`docs/agent-rules/angular-scaffold.md`) para executar o scaffold da Fase 1 dentro deste repositório já existente — garante `--skip-git` (sem repositório Git aninhado), SCSS, versão de Node/Angular compatível, e aplica a configuração de IA do Angular CLI para Claude/Copilot. |
| 06/09/2026 | Versão de impressão (`src/printer/index.html`) removida do escopo, substituída por um CV para download nos três idiomas — o idioma do arquivo baixado acompanha o idioma atual do site. Forma de geração/padronização do download ainda não decidida (Fase 4). Atualizado escopo de conteúdo, Fase 2, Fase 4 e Fase 7. |
| 06/09/2026 | Criado o agente `angular-components` (`docs/agent-rules/angular-components.md`) para gerar os componentes Angular da Fase 2 a partir do que já está documentado em `docs/design-system.md` (estrutura/layout do Figma, tokens, assets) — não se conecta ao Figma (isso é do `designer`) nem cria o projeto (isso é do `angular-scaffold`), só consome o que os dois já produziram. |
| 06/09/2026 | Suporte a tablet adicionado ao escopo (Johnny confirmou), mesmo sem nenhum frame de tablet no Figma (só Desktop 1440/Mobile 375). Breakpoint proposto ~768–1023px (convenção comum, não extraída do Figma) — layout intermediário fica para decisão de implementação na Fase 2. Atualizado decisões arquiteturais, escopo de conteúdo, Fase 2, Fase 7 e critérios de sucesso. |
| 06/09/2026 | Fase 1 (scaffold) executada pelo agente `angular-scaffold`. Nome do diretório `angular-app/` confirmado (Fase 0). `ng new` rodado com Angular CLI **22.1.7** / `@angular/core` **22.1.5** / Node **24.18.0** (`npx @angular/cli@22.1.7 new angular-app --directory=angular-app --style=scss --routing --ssr --skip-git --ai-config=claude-code --ai-config=vscode --package-manager=npm --interactive=false`), gerando `angular-app/` com roteamento, SSR/prerender e SCSS. Node pinado só para o subprojeto (`angular-app/.nvmrc` = `24.18.0`, `engines.node` em `angular-app/package.json`), sem tocar no `.nvmrc` da raiz. Config de IA do Angular CLI aplicada para Claude Code (`CLAUDE.md` + `.mcp.json`) e VSCode/Copilot (`AGENTS.md` + `.vscode/mcp.json` — na v22.1.7 do CLI não existe mais uma opção `ai-config` dedicada a "copilot", `vscode` é a mais próxima). Lint configurado via `ng add @angular-eslint/schematics@22.2.0`; Prettier (já gerado pelo `ng new`) com scripts `format`/`format:check` adicionados; projeto formatado uma vez. Build (`ng build`) e lint validados com sucesso. Pipeline de CI mínimo criado em `.github/workflows/Angular-App-CI.yaml` (só roda em mudanças dentro de `angular-app/**`, instala + lint + build, sem deploy), sem alterar `Develop.yaml`/`Production.yaml`. Efeito colateral necessário: o `.gitignore` da raiz ignorava `.vscode` em qualquer profundidade — restringido para `/.vscode` (só a raiz), para não silenciar o `angular-app/.vscode/` (tasks/launch/extensions/mcp.json), que é intencionalmente versionado pelo scaffold do Angular CLI. Nota de ambiente: a instalação de pacotes (`npm install`) nesta máquina esbarrou numa política de scripts do npm do usuário (`~/.npmrc`, config `allow-scripts` restrita a `@anthropic-ai/claude-code`, erro `EALLOWSCRIPTS`) — resolvido adicionando `angular-app/.npmrc` com `ignore-scripts=true` (validado: build/lint funcionam normalmente sem lifecycle scripts; reavaliar se algum pacote futuro exigir postinstall de verdade). Nenhum `git commit`/`push` foi feito — scaffold fica como arquivos não commitados para revisão do Johnny. |
| 06/09/2026 | Fase 2 (design system), primeira fatia, executada pelo agente `angular-components`: fundação de tema + 2 subcomponentes mais reutilizados + 2 seções mais simples. Tokens de `docs/design-tokens.css` migrados para `angular-app/src/styles/` (`_tokens.scss` cores light/dark, tipografia, sombras, espaçamento e uma pequena escala de border-radius nova — 8px/12px, observada na seção 8 do design-system.md; `_typography.scss` com um mixin por estilo tipográfico; `_breakpoints.scss` com o breakpoint de tablet já proposto na seção 7; `_layout.scss` com os mixins de padding de seção/container reaproveitáveis pelas próximas fatias), consumidos por `src/styles.scss` (reset mínimo + `stylePreprocessorOptions.includePaths` em `angular.json` para import curto nos componentes) e pela fonte Inter carregada via Google Fonts em `src/index.html`. Gerados como standalone components: `IconButton` (`shared/components/icon-button/`, 2 variantes de tamanho, hover/active por convenção do projeto — sem spec de estado no Figma) e `Tag` (`shared/components/tag/`); `Header` (`layout/header/`, logo placeholder "SS" do próprio template Figma, nav de 4 links, divider, Icon Button de tema/menu + botão "Download CV") e `Footer` (`layout/footer/`, ícone `icon-footer` + copyright), ambos responsivos (80px/16-24px desktop → 16px horizontal mobile → 48px tablet interpolado, vertical mantido fixo por serem barras compactas — convenção documentada nos comentários do SCSS, não extração do Figma). Ícone de tema/menu do Header segue pendente de exportação (mesma pendência já registrada em design-system.md 6/8.3) — usado um placeholder SVG genérico inline, comentado como tal. `App` (`app.ts`/`app.html`) atualizado para compor Header + Footer + `router-outlet`, substituindo o template de demo do scaffold. `npm run format`, `npm run lint`, `ng build` (com prerender) e `npm run test` (vitest) validados sem erros. Nenhum `git commit` feito. |
| 06/09/2026 | Fase 2 (design system), T6 do plano de implementação, executada pelo agente `angular-components`: componente **Hero** (`angular-app/src/app/features/hero/hero.{ts,html,scss,spec.ts}`), a partir de `docs/design-system.md` seção 8.8, com conteúdo real (nome "Johnny Souto", cargo "Analista Desenvolvedor de Sistemas", bio-rascunho com anos de experiência calculados a partir de Nielsen do Brasil/Jan-2006 — 20 anos em 2026, função pura `calculateExperienceYears` testada isoladamente), GitHub real com ícone (`icon-social-github`) e LinkedIn como link de texto (sem ícone no Figma). Foto substituída por bloco neutro com iniciais "JS", reaproveitando a composição em camadas (retângulo deslocado + borda) da spec. Bloco "Location + Hire" da spec 8.8 **omitido** (sem dado real, não é decisão da seção 10 — sinalizado para o Johnny revisar). Adicionado mixin `layout.section-padding` (`angular-app/src/styles/_layout.scss`), reutilizável pelas próximas seções de conteúdo. Efeito colateral no `IconButton` (`shared/components/icon-button/`): ganhou um `href` opcional para funcionar como link externo real (`<a>` em vez de `<button>`, escolha feita via `@if`/`@else`, já que aninhar `<button>` dentro de `<a>` seria HTML inválido) — expôs um bug real do Angular 22.1.5 (`<ng-content>` duplicado nos 2 branches de um `@if`/`@else` falha a projeção de conteúdo silenciosamente), contornado com `ngTemplateOutlet` e coberto por teste novo (ver design-system.md, log de 06/09/2026, entrada do Hero, para o detalhe técnico completo). `npm run lint`, `npm run test` (26 testes, todos passando) e `ng build` (com prerender) validados sem erros. Nenhum `git commit` feito. |
| 06/09/2026 | Fase 2 (design system), T7/T8 do plano de implementação (`.claude/plans/splendid-stirring-flute.md`), executadas juntas pelo agente `angular-components`: componentes **About** (`angular-app/src/app/features/about/`, design-system.md 8.9) e **Skills** (`angular-app/src/app/features/skills/`, 8.10), integrados em `pages/home/` na ordem Hero → About → Skills. **About**: `host: { id: 'about' }` (âncora `#about` já usada pelo Header); foto sempre à esquerda (sem alternância, confirmado na spec), composição em camadas espelhada da do Hero (fundo atrás à esquerda/baixo, foto à direita/cima, mesmo bloco neutro + iniciais "JS" — sem foto real, seção 10); bio-rascunho expandida a partir da do Hero (mesma ressalva de rascunho a revisar); checklist "quick bits" (2×2) preenchida com 4 fatos **reais e verificáveis** contra `src/pt/index.html` — anos de experiência (mesma função `calculateExperienceYears` do Hero, reaproveitada/importada), formação (Uninove, Gestão da Tecnologia da Informação), inglês intermediário e estado civil (casado, um filho — texto literal do legado). O candidato "localização/stack" sugerido no pedido não foi usado: sem cidade documentada em lugar nenhum do repo (grep confirmado) e "stack" duplicaria a seção Skills — substituído pelo estado civil (fato real disponível, sem duplicar outra seção). **Skills**: conteúdo real agrupado por categoria (Back-End, Banco de Dados, Front-End, Versionamento, Pessoais), replicando a categorização de `src/pt/index.html` (20 itens reais no total, nenhum inventado); só 3 itens com ícone real (`icon-javascript`, `icon-nodejs`, `icon-git`, decisão já fechada na seção 10). Grid do Figma (flat, 2 linhas × 8 itens, `justify-between`, sem categorias) **adaptado** para uma linha `flex-wrap` por categoria (contagem real varia 5/3/7/1/4 por categoria — `justify-between` deixaria "Versionamento", com 1 item só, esticado de forma estranha) — decisão de UI por convenção, não extração do Figma, comentada em `skills.ts`/`skills.scss`. Achado da seção 8.10 (item `Tech` sem variante texto-only, `justify-center` desalinha o label quando falta ícone) resolvido escolhendo a saída (a) já documentada na spec: slot de ícone com altura fixa reservada (`--space-skills-icon-slot`, 64px) sempre renderizado, com ou sem `<img>` dentro — mantém os labels alinhados entre si sem duplicar o item em variantes "com/sem ícone" (justificativa completa no comentário de `skills.scss`). Novo token `--space-skills-icon-slot` adicionado a `_tokens.scss` (alias de `--space-64`, mesma convenção de nomear semanticamente um valor já existente na escala). Testes unitários novos cobrindo lógica não-trivial de ambos (39 testes no total, todos passando): About (id do host, cálculo de anos reaproveitado, 4 quick bits com o texto real esperado, ausência de `<img>` no bloco de foto); Skills (5 categorias na ordem certa, contagem total de itens, exatamente 3 ícones renderizados com o `src` correto, os 17 itens restantes texto-only com slot de ícone presente). `npm run lint`, `npm run test` e `ng build` (com prerender — "Prerendered 1 static route", `id="about"` e as 5 categorias confirmadas no HTML gerado) validados sem erros. Nenhum `git commit` feito. |
| 06/09/2026 | Fase 2 (design system), T9/T10 do plano de implementação, executadas juntas pelo agente `angular-components`: componentes **Experience** (`angular-app/src/app/features/experience/`, design-system.md 8.6/8.11) e **Work** (`angular-app/src/app/features/work/`, 8.5), integrados em `pages/home/` na ordem Hero → About → Skills → Experience → Work. **Experience**: heading Tag + `Subtitle/Normal` (8.11, não H2) com `gap: 48px` uniforme entre heading/item/item; item card 896px/radius 12/shadow-md/padding 32px, sem conector visual de timeline (8.6, reconfirmado ausente). Conteúdo real com as 6 entradas completas da "Experiência Profissional" de `src/pt/index.html` (Coca-Cola FEMSA, Telefônica Brasil ×2 — Analista SOC Ago2019-Mai2021 e Analista OSS Ago2017-Ago2019, tratados como 2 itens de timeline separados —, Telefônica Educação Digital, RCS Sistemas, Nielsen do Brasil), bullets reaproveitando o texto real de cada `<li>` do legado (sem inventar atividade). Logo de empresa (102×28 na spec) virou texto simples (seção 10) numa coluna de largura mínima fixa (160px, decisão de implementação). **Work**: card 100% coberto pela spec 8.5, sem lacuna nova — heading raiz da seção (Tag+Subtitle centralizado) resolvido por inferência do mesmo padrão repetido nas outras 4 seções de conteúdo com esse formato (About/Skills/Experience/Contact me), já que o Figma não tem uma subseção própria documentando a raiz do Work (registrado em design-system.md como achado, não como lacuna bloqueante). Imagem vira bloco neutro (seção 10); tags de tecnologia extraídas da descrição real de cada projeto (Pitcher → MS-SQL Server/SAP/Salesforce; ProFuturo → HTML5/CSS3/JavaScript/jQuery/PIXI.js/Grunt/GitLab), nenhuma inventada. **Decisão sobre a ação do card** (Icon Button "ver projeto"): resolvida caso a caso, não com uma regra única — Pitcher não tem link público no legado, ação omitida; ProFuturo já tinha um link real no legado ("Leia mais...", página da Fundação Telefônica Vivo sobre o projeto), reaproveitado como ação do card, em vez de linkar os 2 cards para um perfil genérico (GitHub/LinkedIn do Johnny) sem relação direta com "ver este projeto". Ícone interno do Icon Button de ação é um SVG genérico de "link externo" (mesma convenção do ícone de tema/menu do Header — placeholder inline comentado como tal, sem asset exportado do Figma para esse ícone). Testes unitários novos cobrindo lógica não-trivial de ambos (15 testes no total, todos passando): Experience (ordem/conteúdo dos 6 itens, Telefônica Brasil como 2 itens distintos, contagem de bullets, ausência de `<img>` de logo); Work (id do host, os 2 projetos reais na ordem certa, tags corretas por projeto, ação omitida no Pitcher/presente com o href real no ProFuturo, ausência de `<img>` de thumbnail). `npm run lint`, `npm run test` (54 testes no total do projeto) e `ng build` (com prerender — "Prerendered 1 static route", `id="work"` e os textos "Pitcher"/"ProFuturo" confirmados no HTML gerado) validados sem erros. Nenhum `git commit` feito. |
| 06/09/2026 | Fase 2 (design system), T11/T12 do plano de implementação, executadas juntas pelo agente `angular-components`: componentes **Testimonials** (`angular-app/src/app/features/testimonials/`, design-system.md 8.7) e **Contact me** (`angular-app/src/app/features/contact-me/`, 8.12), integrados em `pages/home/` na ordem final Hero → About → Skills → Experience → Work → Testimonials → Contact me (as 7 seções de conteúdo agora completas). **Testimonials**: `host: { id: 'testimonials' }`; heading (Tag + Subtitle/Normal) e fundo `--color-gray-50` da raiz da seção resolvidos por inferência do padrão alternado já confirmado nas outras seções (mesma inferência já registrada no Work), já que o Figma não documenta a raiz da seção, só o card (8.7); card 100% coberto pela spec (~373px, radius 12, shadow-md, padding 48px, avatar 64×64 com ícone genérico dentro). Conteúdo 100% fictício (seção 10, decisão já fechada): 3 depoimentos claramente marcados como placeholder, de duas formas — nomes literais "Depoimento de exemplo 1/2/3" (em vez de nomes humanos inventados que pudessem ser confundidos com pessoas reais) **e** uma nota visível no subtítulo do heading ("Depoimentos de exemplo — conteúdo temporário..."), perceptível a qualquer revisor olhando a página renderizada, não só o código-fonte. Achado registrado: as medidas do ícone do avatar na extração original (8.7: avatar 64×64, ícone "40×40" com padding 20px) são incompatíveis entre si num círculo de 64px (40 + 2×20 = 80 ≠ 64) — resolvido com um ícone de 32×32 centralizado por flexbox, decisão de implementação documentada no componente, não uma nova extração do Figma. Novo token `--color-testimonial-avatar-bg` (`#9ca3af`) adicionado a `_tokens.scss`, fixo nos dois temas (mesmo critério já usado para outros valores soltos sem token formal no Figma, seção 8.7: "não corresponde a nenhum token cinza da seção 3"). **Contact me**: `host: { id: 'contact' }`; tudo empilhado numa única coluna central (heading → email → telefone → social), sem colunas lado a lado, 100% coberto pela spec 8.12. Conteúdo real: email `johnnyjns@gmail.com` (ícone `icon-mail`) e WhatsApp `+55 11 99703-7799` como link `https://wa.me/5511997037799` (ícone `icon-phone`), cada um com um Icon Button de "copiar" (variante grande 44×44, ícone `icon-copy`) ao lado; GitHub `github.com/johnnysoutodev` com o ícone real (`icon-social-github`, mesmo padrão de Icon Button-link já usado no Hero); LinkedIn `linkedin.com/in/johnnysouto` como link de texto simples, sem ícone (decisão já confirmada em 8.12/10 — nenhum ícone de LinkedIn existe no arquivo Figma). Decisão de implementação registrada: email/telefone viram links reais (`mailto:`/`https://wa.me/...`), não só texto estático — não é uma extração da spec 8.12 (que só documenta o conteúdo visual), mas consistente com GitHub/LinkedIn já serem links reais desde o Hero. **Ação de copiar implementada de verdade** (Clipboard API do browser, `navigator.clipboard.writeText`): método `copy()` guardado por `isPlatformBrowser(inject(PLATFORM_ID))` (mesmo padrão do `ThemeService`, `core/theme/theme.ts`) — na prática o método só roda a partir de um `(click)` do template (eventos de template nunca disparam durante o prerender/SSR), mas a guarda foi mantida mesmo assim, defensiva, seguindo a convenção do projeto em vez de assumir "só roda no browser na prática". Feedback visual temporário (signal `copiedField`, ~2s via `setTimeout` limpo em `DestroyRef.onDestroy`): o ícone do botão de copiar troca para um checkmark inline (sem asset exportado do Figma para esse estado, mesma convenção de placeholder inline já usada em outros ícones não cobertos pela spec) e o `aria-label` muda de "Copiar email"/"Copiar telefone" para "Email copiado!"/"Telefone copiado!" enquanto ativo; falha silenciosa (try/catch) se a Clipboard API for negada/indisponível. Testes unitários novos cobrindo lógica não-trivial de ambos (17 testes no total, todos passando): Testimonials (host id, 3 cards com nomes fictícios na ordem certa, nota de placeholder visível, ausência de `<img>` no avatar); Contact me (host id, layout empilhado de fato — ordem dos filhos diretos do container —, links reais de email/WhatsApp/GitHub/LinkedIn com os hrefs corretos, ação de copiar via Clipboard API mockada — incluindo reversão do feedback após o timeout e rejeição silenciosa —, e um bloco de testes à parte com `PLATFORM_ID` sobrescrito para `'server'` confirmando que `navigator.clipboard` nunca é tocado no SSR). `npm run lint`, `npm run test` (71 testes no total do projeto) e `ng build` (com prerender — "Prerendered 1 static route", `id="testimonials"`/`id="contact"`, os 3 nomes fictícios, o email, o link `wa.me` e `linkedin.com/in/johnnysouto` confirmados no HTML gerado) validados sem erros. Nenhum `git commit` feito. |
| 06/09/2026 | Fase 2 (design system), T13 do plano de implementação (`.claude/plans/splendid-stirring-flute.md`), executada pelo agente `angular-components`: **menu mobile dedicado** (novo componente `MobileMenu`, `angular-app/src/app/layout/mobile-menu/`, design-system.md seção 8.13) + botão de hamburguer novo em `layout/header/`. Painel ancorado à direita (`right: 0`, `width: min(85%, 320px)` — reproduz proporcionalmente os "320px de 375 totais" medidos só no frame mobile do Figma, com um teto maior em `breakpoints.tablet-only` já que o Figma não tem referência de tablet, seção 7/9.4), scrim com `blur(4px)` + novo token `--color-mobile-menu-scrim` (10% de opacidade sobre `--color-gray-900`/`--color-gray-dark-900`, um valor por tema em `_tokens.scss`), sombra `--shadow-2xl-dark` (nome confirmado, já existia em `_tokens.scss` desde a fatia de fundação). 3 blocos internos (Logo+fechar/Links/Switch Theme+Download CV) com os mesmos 4 links do Header, agora extraídos para uma fonte única compartilhada (`layout/nav-links.ts`, `NAV_LINKS`) — evita duplicar rótulos/ordem em 2 componentes. Novo token de tipografia `--font-heading-h3-bold-mobile-*` (24/32/-2%, mixin `heading-h3-bold-mobile` em `_typography.scss`) para o logo do painel, spec própria de 8.13 fora da tabela da seção 4. Ícone de tema trocando sol↔lua dentro do menu mobile (confirmado visualmente na spec 8.13, ao contrário do ícone do Header, que segue estático — assimetria documentada no comentário do template, a revisitar); ícones de fechar/hamburguer/X são SVGs genéricos inline, mesma convenção de placeholder já usada no ícone de tema do Header (sem asset exportado do Figma, seções 6/8.3). **Estado de abertura/fechamento** (decisão de implementação, não há frame "fechado" nem gatilho de abertura no Figma, mesma limitação já registrada em 8.13): signal `isMobileMenuOpen` local no `Header` (dono do botão hamburguer), descendo como `[open]` para o `MobileMenu` — componente controlado, sem estado próprio —, que só emite `(closed)` (scrim clicado, Escape, botão "X", link clicado) para o Header decidir. Transição (slide-in do painel + fade do scrim) e bloqueio de scroll do body enquanto aberto são decisões de UX razoáveis, não extraídas do Figma. **Acessibilidade** (WCAG AA, exigida em `angular-app/CLAUDE.md`, não é spec do Figma): `aria-expanded`/`aria-controls`/`aria-label` dinâmico no botão de hamburguer do Header; foco movido pro primeiro elemento focável do painel ao abrir e devolvido pro elemento que tinha foco antes ao fechar; focus trap básico (Tab/Shift+Tab preso dentro do painel via handler de teclado, sem depender de bibliotecas); `Escape` fecha; scrim clicável fecha; painel marcado `inert` enquanto fechado (fora da árvore de acessibilidade e não focável) via `[attr.inert]` ligado ao mesmo signal `open`. **SSR-safety**: `effect()` do `MobileMenu` (usado pro focus management + scroll lock) guardado por `isPlatformBrowser(inject(PLATFORM_ID))`, mesmo padrão já obrigatório desde o `ThemeService` — coberto por um bloco de testes à parte com `PLATFORM_ID` sobrescrito para `'server'`. 21 testes novos entre `Header` (hamburguer abre/fecha, `aria-expanded`/`aria-controls`, propagação do `[open]` pro `MobileMenu`, fechamento via `(closed)`) e `MobileMenu` (abre/fecha, `inert`, scroll lock, scrim/botão-X/link/Escape fecham, foco movido ao abrir e devolvido ao fechar, focus trap nas duas bordas, troca de ícone sol/lua, e o bloco de SSR-safety) — 92 testes no total do projeto (todos passando). `npm run lint`, `npm run test` e `ng build` (com prerender — `id="mobile-menu"` e `.header__menu-toggle` confirmados no HTML gerado, "Prerendered 1 static route") validados sem erros. Nenhum `git commit` feito. |
| 06/09/2026 | **T14 (QA final da Fase 2) — feito diretamente, sem subagente** (decisão explícita por custo de token: cada chamada do `angular-components` estava custando 150-180 mil tokens por já reler a documentação inteira e escrever testes/docs extensos a cada vez; T14 é só validação, não precisava desse custo de novo). Revisão manual no navegador (não só o relatório do subagente do T13) encontrou um bug real no menu mobile: o `.focus()` que move o foco pro painel ao abrir rodava antes do binding `[attr.inert]` do template remover o `inert` do painel (mesmo signal `open`, sem ordem garantida entre o `effect()` e a change detection do template) — o navegador recusa focar um elemento `inert`, então o foco ficava preso no `<body>`. Não pego pelos testes porque o helper de teste chamava `detectChanges()` (aplica o binding) antes de `flushEffects()` (roda o `.focus()`), mascarando a race. Corrigido com `queueMicrotask()` em `mobile-menu.ts`, e o teste ajustado (`await Promise.resolve()` após abrir) pra exercitar o timing real em vez de continuar mascarando — commitado junto com o T13 (`b422b3f`). Validação final: `npm run lint`/`npm run test` (92/92) /`ng build` com prerender, todos limpos; conferido manualmente no navegador (Chrome via automação) — tema claro e escuro renderizando corretamente em todas as 7 seções + Header/Footer, navegação por âncora (clique em "Work" no nav pula direto pra `#work`) funcionando, menu mobile abre/fecha/foca corretamente (forçando visibilidade via `style.display` pra contornar uma limitação da ferramenta de automação de navegador, que não redimensiona o viewport de verdade — o comportamento responsivo em si foi conferido por revisão de código dos mixins de breakpoint já reutilizados em todos os componentes, não por screenshot em cada largura). Checklist da Fase 2 (esta seção) marcado como 100% concluído. Nenhum `git commit` feito nesta entrada de log (só a atualização deste documento) — a atualização dos docs entra no próximo commit que o Johnny pedir. |
| 07-08/09/2026 | Sessão longa de conteúdo/mídia real e QA, a pedido do Johnny, feita diretamente sem subagente. **Fotos reais** adicionadas no Hero e no About (About depois trocado por uma foto neutra/não-retrato por privacidade; texto do estado civil ajustado pra não citar o nome do filho). **Experience**: logos reais de empresa pra quase todas (RCS Sistemas continua texto, sem arquivo disponível); alinhamento da coluna logo/texto corrigido (largura fixa) e cargo+período trocado de lado-a-lado (quebrava de forma inconsistente entre cards) pra empilhado; Vivo (SOC+OSS) virou 1 card só com 2 posições (`ExperienceItem.positions`) em vez de 2 cards com o mesmo logo duplicado. **Work renomeado para Project** (componente Angular — `features/work/`→`features/project/`, seletor/âncora/classes junto; o frame do Figma continua se chamando "Work", só o lado Angular mudou), com logos reais dos 2 projetos no bloco de imagem (antes um bloco neutro vazio); padding/gap do card corrigidos pra bater com a spec 8.5 (48px em ambas as colunas — radius+shadow própria no card E na imagem confirmadas como intencionais na spec, depois de uma primeira tentativa errada de remover a do card) e `height` fixo trocado por `min-height` (conteúdo real com mais tags que os cards fictícios do Figma vazava pra fora do card) — regras gerais documentadas em `docs/agent-rules/angular-components.md` pra não repetir em componentes futuros, junto com um achado sobre o `NgOptimizedImage` limpar `style` setado direto no elemento que ele gerencia (overrides de tamanho precisam vir de uma custom property CSS herdada de um elemento pai). **Assets corrigidos**: 3 logos com extensão de arquivo errada (eram WebP salvos como `.svg`/`.png`, não renderizavam) renomeados/redimensionados; vários logos redimensionados de fontes muito maiores que o necessário pra evitar o aviso `NG02960` de imagem sobredimensionada. Ver `docs/design-system.md` seção 10 (decisões de conteúdo) para o detalhe completo de cada mudança. `npm run test` (97 testes, todos passando ao final) e QA visual no navegador (luz/escuro, console sem avisos) validados ao longo de toda a sessão. Nenhum `git commit` feito. |
| 08/09/2026 | **Ícone do LinkedIn**, a pedido do Johnny, feito diretamente sem subagente. `icon-social-linkedin.svg` desenhado do zero (sem equivalente no Figma, confirmado ausente em 8.12/10) no mesmo estilo outline dos ícones existentes — usado como Icon Button só-ícone no Hero (par com o GitHub) e no Contact me, onde o texto visível da URL ao lado dos ícones (GitHub e LinkedIn) foi removido por ficar redundante. Nenhum `git commit` feito. |
| 08/09/2026 | **Header sticky + blur reativo ao scroll**, a pedido do Johnny, feito diretamente sem subagente. `position: sticky` + classe `.header--scrolled` (signal `isScrolled`, guardado por `isPlatformBrowser`) aplicando fundo semi-transparente + `backdrop-filter: blur()` + sombra só depois de 8px de scroll — decisão de UX/engenharia, não spec do Figma (que só sugeria via uma borda transparente, "não confirmável", já registrada desde a extração original). **Achado técnico real** (registrado em `docs/agent-rules/angular-components.md` pra não repetir): `position: sticky` simplesmente não funcionava até 2 correções de CSS global fora do componente em si — `app-root` (elemento custom, `display: inline` por padrão do navegador) precisou de `display: block` explícito (`styles.scss`); e o host do próprio `Header` (`app-header`) precisou trocar `display: block` por `display: contents` (`header.scss`) — com `block`, o host encolhe pro tamanho do único filho (68px), virando a "containing block" do sticky com ZERO espaço de scroll pra grudar, então o header simplesmente rolava junto com a página sem erro nem aviso indicando a causa. `npm run test` (100 testes, todos passando) e QA visual no navegador (luz/escuro, scroll real) validados. Nenhum `git commit` feito. |
