# Design System — extraído do Figma

> Specs extraídas via MCP do Figma para a migração Angular (`docs/PLANO-MIGRACAO-ANGULAR.md`, Fase 0/2). Não é fonte de verdade visual — o Figma continua sendo a referência; este documento existe para consulta rápida durante a implementação, sem precisar reconectar ao MCP a cada dúvida.

**Arquivo Figma:** `template_portfolio_website`
**Link:** <https://www.figma.com/design/9z2dzCKhlXWqVynN5SEeEM/template_portfolio_website?node-id=0-1&m=dev&t=p400RgMm8j8SjqJM-1>
**File key:** `9z2dzCKhlXWqVynN5SEeEM`
**Extraído em:** 06/09/2026

> Nota: o conteúdo textual/imagens do arquivo é um template genérico (placeholder "Sagar", projetos fictícios "Fixkit" etc.) — servirá apenas de base visual/estrutural. O conteúdo real (currículo do Johnny) deve substituir os placeholders na Fase 2/4.

## 1. Estrutura de página (única página "Home")

O arquivo tem uma única página, com frames organizados por tema (Light/Dark) e breakpoint (Desktop/Mobile), todos com a mesma estrutura de seções:

| Seção | Node ID (Desktop/Light) | Altura (desktop) | Observação |
| --- | --- | --- | --- |
| Header | `316:588` (instance) | 68px | Fixo/sticky, largura total |
| Hero | `316:194` | 552px | Nome, título, localização, disponibilidade, links sociais, foto |
| About | `316:229` | 962px | Texto "sobre mim" |
| Skills | `316:256` | 560px | Grid de ícones de tecnologias |
| Experience | `316:357` | 1140px | Timeline de experiências profissionais |
| Work | `316:415` | 1848px | Cards de projetos |
| Testimonials | `316:510` | 740px | Depoimentos |
| Contact me | `316:537` | 560px | Email/telefone/redes sociais |
| Footer | `316:579` | 68px | Copyright |

Sections ficam empilhadas sem gap entre si (cada uma já inclui seu próprio padding vertical — valores medidos na seção 2, "Padding de seção").

### Frames/variantes disponíveis

- `328:4104` — **Light Mode** (grupo com todas as variantes light)
  - `316:177` — Home / Desktop / Light (1440×6498)
  - `327:417` — Home / Mobile (iPhone 8) / Light (375×9382)
  - `327:1337` — Home / Mobile (iPhone 8) / Light / Mobile Menu (overlay do menu mobile)
- `328:4105` — **Dark Mode** (mesma estrutura, sufixo Dark)
  - `327:1805` — Home / Desktop / Dark
  - `327:2147` — Home / Mobile (iPhone 8) / Dark
  - `327:3601` — Home / Mobile (iPhone 8) / Dark / Mobile Menu

Ou seja: o design já prevê **dark mode** e **menu mobile** dedicado — vale decidir se isso entra no escopo da migração (o plano atual não menciona dark mode explicitamente).

## 2. Layout / grid

- **Desktop:** frame de 1440px de largura; container de conteúdo com `x=80` e `width=1280` → padding lateral de **80px**, conteúdo com largura máxima de **1280px**.
- **Mobile:** frame de referência 375px (iPhone 8).

### Tokens formais de espaçamento no Figma

**Verificado (06/09/2026):** o arquivo **não tem variáveis de espaçamento nomeadas** (nada equivalente a `Spacing/2`, `Gap/sm` etc.). Confirmado via `get_variable_defs` em 4 frames de seção distintos (Hero `316:194`, About `316:229`, Skills `316:256`, Work `316:415`) — em todos os casos o retorno só trouxe variáveis de **cor** (`Gray/*`), **tipografia** (`Heading/*`, `Body*/*`, `Subtitle/*`) e **efeito** (`Drop Shadow/*`), nunca uma variável de espaçamento. Ou seja, ao contrário de cor/tipografia (que são tokens reutilizáveis definidos como variáveis do Figma), o espaçamento no arquivo é definido **diretamente nos valores de padding/gap do Auto Layout** de cada frame — valores "soltos", sem nome/token por trás. A tabela abaixo é, portanto, espaçamento **observado por amostragem**, não uma lista de tokens formais.

### Padding de seção (medido via `get_design_context`)

**Cobertura completa: as 9 seções do template foram medidas.** Primeira amostra (Hero, About, Skills, Work) confirmada em extração anterior; completada nesta rodada (06/09/2026) com Header, Experience, Testimonials, Contact me e Footer — todas com a mesma estrutura raiz `padding lateral / padding vertical`:

| Seção | Node ID | Padding horizontal | Padding vertical |
| --- | --- | --- | --- |
| Header | `316:588` | 80px | 16px |
| Hero | `316:194` | 80px | 96px |
| About | `316:229` | 80px | 96px |
| Skills | `316:256` | 80px | 96px |
| Experience | `316:357` | 80px | 96px |
| Work | `316:415` | 80px | 96px |
| Testimonials | `316:510` | 80px | 96px |
| Contact me | `316:537` | 80px | 96px |
| Footer | `316:579` | 80px | 24px |

**Resultado:** 7 das 9 seções (Hero, About, Skills, Experience, Work, Testimonials, Contact me) usam exatamente o mesmo padding de seção — **80px horizontal / 96px vertical** — confirmando que esse é o padrão do template para seções de conteúdo. **Header e Footer são exceção esperada**, e a exceção se confirmou: como têm altura fixa de 68px (barras finas, sticky/fixo no caso do Header), não cabe padding vertical de 96px — o valor real medido é **16px vertical no Header** e **24px vertical no Footer** (ambos mantêm os mesmos **80px horizontal**). O padding lateral de 80px é, portanto, universal nas 9 seções; o padding vertical de 96px é o padrão das 7 seções de conteúdo, com Header/Footer usando valores reduzidos e distintos entre si (16px vs. 24px — não há um segundo padrão comum entre eles).

Dentro de cada seção, o elemento `Container` direto costuma ter ainda um `padding: 0 32px` próprio (inset adicional além dos 80px da seção) — confirmado em Hero, About, Skills, e agora também em Header, Experience, Testimonials, Contact me e Footer (ou seja, 8 das 9 seções); em Work o container não tem esse padding extra (única exceção).

Margem entre seções: **0** — confirmado agora para as 9 seções via `get_metadata` no frame `316:177` (Home / Desktop / Light): a posição `y` de cada seção bate exatamente com o fim da anterior (Header 0–68 → Hero 68–620 → About 620–1582 → Skills 1582–2142 → Experience 2142–3282 → Work 3282–5130 → Testimonials 5130–5870 → Contact me 5870–6430 → Footer 6430–6498, igual à altura total do frame). Não há gap entre nenhuma das 9 — o espaçamento visual entre seções vem inteiramente do padding vertical de cada uma (96px nas 7 de conteúdo, 16px/24px em Header/Footer).

### Padding de seção — Mobile (medido via `get_design_context`)

Amostra de 4 seções no frame `327:417` (Home / Mobile (iPhone 8) / Light, ver seção 1) — a mesma amostra original usada no desktop (Hero, About, Skills, Work), para manter a comparação consistente. Node IDs são específicos do frame mobile (diferentes dos IDs do desktop, mesmo sendo a "mesma" seção):

| Seção | Node ID (Mobile) | Padding horizontal | Padding vertical |
| --- | --- | --- | --- |
| Hero | `327:419` | 16px | 64px |
| About | `327:442` | 16px | 64px |
| Skills | `327:468` | 16px | 64px |
| Work | `327:638` | 16px | 64px |

**Resultado:** as 4 seções amostradas usam exatamente o mesmo padding no mobile — **16px horizontal / 64px vertical** — mesma estrutura do desktop (padding lateral + padding vertical simétrico no próprio Auto Layout da seção, sem token nomeado), só com valores menores: o horizontal cai de 80px para 16px (5x menor), o vertical cai de 96px para 64px (1,5x menor). Confirmado via `get_design_context` em Hero (`327:419`, classe gerada `px-[16px] py-[64px]`) e Skills (`327:468`, mesma classe); About (`327:442`) e Work (`327:638`) confirmados por geometria equivalente via `get_metadata` (Container interno a `x=16`/`y=64` dentro do frame de seção, com a mesma folga simétrica do lado oposto).

### Espaçamento observado (gaps e paddings internos)

Valores de `gap`/`padding` do Auto Layout encontrados nas 9 seções (amostra original de 4 — Hero, About, Skills, Work — completada em 06/09/2026 com Header, Experience, Testimonials, Contact me e Footer), do menor ao maior. **Não são tokens nomeados** — são os valores em pixels tal como aparecem no Figma:

| Valor | Uso típico observado | Onde aparece |
| --- | --- | --- |
| 4px | `gap` entre ícones sociais (Links) | Hero, Contact me |
| 6px | `padding` do Icon Button (botão de ícone circular) | Hero, Work, Header, Contact me |
| 8px | `gap` ícone+texto (localização, disponibilidade, tech icon+label, tags de tecnologia; ícone+nota de copyright) | Hero, Skills, Work, Footer |
| 10px | `gap` entre colunas da checklist "quick bits" | About |
| 16px | `gap` entre título e subtítulo de uma seção (Row com Tag + heading); também `gap` do bloco `Actions` do Header (Icon Button + Botão) | Skills, Work, Header, Experience, Testimonials, Contact me |
| 16px | `padding` vertical da seção (versão compacta, altura fixa de 68px — não cabe o padrão de 96px) | Header |
| 20px/4px | `padding` horizontal/vertical do componente Tag (badge) | About, Skills, Work |
| 20px | `gap` entre ícone, texto e botão de ação numa linha de contato (Email/Phone) — mesmo valor do Tag acima, mas uso de `gap`, não `padding` | Contact me |
| 24px | `gap` entre blocos de conteúdo (texto+parágrafos; título+descrição+tags+ações de um card; avatar+depoimento+dados do cliente); `gap` da navegação do Header | About, Work, Header, Testimonials |
| 24px | `padding` vertical da seção (versão compacta, altura fixa de 68px — não cabe o padrão de 96px; valor diferente do Header) | Footer |
| 32px | `padding` horizontal do `Container` interno de uma seção (inset adicional além dos 80px) | Hero, About, Skills, Header, Experience, Testimonials, Contact me, Footer |
| 32px | `padding` interno (nas 4 bordas) do card de item da Experience — mesmo valor do inset acima, mas aplicado como padding completo do card, não só horizontal | Experience |
| 48px | `gap` macro entre as colunas/linhas principais de uma seção (o espaçamento mais recorrente do arquivo) | Hero, About, Skills, Work, Experience, Testimonials, Contact me |
| 48px | `padding` interno dos cards (projeto no Work; depoimento na Testimonials) | Work, Testimonials |
| 80px | `padding` horizontal da seção — único valor universal nas 9 seções, inclusive Header e Footer | Hero, About, Skills, Work, Experience, Testimonials, Contact me, Header, Footer |
| 96px | `padding` vertical da seção (padrão das seções de conteúdo; Header/Footer usam 16px/24px, ver linhas acima) | Hero, About, Skills, Work, Experience, Testimonials, Contact me |

> Nota: os valores 6px e 10px quebram a hipótese anterior de "múltiplos de 8px" — a escala real é mais próxima da escala padrão do Tailwind (4, 6, 8, 10, 16, 20, 24, 32, 48...) do que de uma progressão estrita em base 8. A extração anterior (amostragem pontual) tinha generalizado isso incorretamente; esta tabela substitui aquela observação. Com a cobertura das 9 seções (06/09/2026), nenhum valor de pixel novo apareceu além dos já listados na amostra original — a escala se confirmou estável; a única novidade é o **padding vertical reduzido e distinto** de Header (16px) e Footer (24px), que já era esperado por serem barras de altura fixa (68px) em vez de seções de conteúdo.

### Artefato de tokens CSS

Os valores de cor (seção 3), tipografia (seção 4), sombra (seção 5) e a escala de espaçamento observada (acima) foram traduzidos mecanicamente para CSS custom properties em **[`docs/design-tokens.css`](./design-tokens.css)** — artefato agnóstico de framework, sem valor novo (só formato). Fica em `docs/` (fora de `src/`/`public/`) porque o projeto Angular ainda não existe; quando `angular-scaffold`/`angular-components` rodarem, esse conteúdo migra para dentro do projeto Angular. Convenções adotadas (tema via `[data-theme="dark"]`, espaçamento nomeado pelo próprio valor em px) estão documentadas no cabeçalho do próprio arquivo.

## 3. Cores (variáveis do Figma)

### Light mode

| Token | Valor |
| --- | --- |
| Gray/Default | `#ffffff` |
| Gray/50 | `#f9fafb` |
| Gray/100 | `#f3f4f6` |
| Gray/200 | `#e5e7eb` |
| Gray/600 | `#4b5563` |
| Gray/700 | `#374151` |
| Gray/900 | `#111827` |

### Dark mode

| Token | Valor |
| --- | --- |
| Gray/Dark/Default | `#030712` |
| Gray/Dark/50 | `#111827` |
| Gray/Dark/100 | `#1f2937` |
| Gray/Dark/200 | `#374151` |
| Gray/Dark/600 | `#d1d5db` |
| Gray/Dark/700 | `#e5e7eb` |
| Gray/Dark/900 | `#f9fafb` |

> Padrão claro de dark mode: a escala é essencialmente invertida (900↔Default, 700↔600 trocam de papel), típico de paleta Tailwind Gray com tema dark por inversão de escala.

## 4. Tipografia

Família única: **Inter**.

| Token | Peso | Tamanho | Line-height | Letter-spacing |
| --- | --- | --- | --- | --- |
| Heading/H1/Bold | 700 (Bold) | 60px | 72px | -2 |
| Heading/H2/Semi Bold | 600 (Semi Bold) | 36px | 40px | -2 |
| Heading/H3/Semi Bold | 600 (Semi Bold) | 30px | 36px | -2 |
| Subtitle/Semi Bold | 600 (Semi Bold) | 20px | 28px | 0 |
| Subtitle/Normal | 400 (Regular) | 20px | 28px | 0 |
| Body1/Normal | 400 (Regular) | 18px | 28px | 0 |
| Body2/Medium | 500 (Medium) | 16px | 24px | 0 |
| Body2/Normal | 400 (Regular) | 16px | 24px | 0 |
| Body3/Medium | 500 (Medium) | 14px | 20px | 0 |
| Body3/Normal | 400 (Regular) | 14px | 20px | 0 |

Todos os valores acima são idênticos entre Light e Dark (só a cor de texto muda, via tokens de cinza da seção 3).

> **Verificação (06/09/2026):** confirmado via `get_design_context` em nós de texto de 3 seções distintas (Hero `316:194`, About `316:229`, Experience `316:357`) que a família de fonte é **exclusivamente "Inter"** (variantes Regular/Medium/Semi Bold/Bold), sem mistura com nenhuma outra fonte. Por ser uma Google Font padrão, conforme critério de `docs/agent-rules/designer.md` (seção "3.1 Extrair assets"), **não é necessário exportar arquivo físico de fonte** — o nome da família documentado acima já é suficiente.

> **Correção de unidade do letter-spacing (06/09/2026):** a coluna "Letter-spacing" acima ("-2"/"0") está em **porcentagem do tamanho da fonte**, não em pixels — confirmado via `get_design_context` no nó do headline do Hero (`316:203`), cujo estilo nomeado é literalmente "Heading/H1/Bold - Desktop" com `letterSpacing: -2` na metadata do Figma, e que renderiza como `-1.2px` num texto de `60px` (1.2 ÷ 60 = 2%). Ou seja, `-2` = `-2%` do `font-size` de cada linha da tabela (H1 60px → -1.2px; H2 36px → -0.72px; H3 30px → -0.6px — este último também observado num segundo nó, o logo do Header, com peso Bold no mesmo tamanho). Os valores "0" (Subtitle/Body) não são afetados pela unidade. Ver `docs/design-tokens.css` para a tradução em `em` (`-2% = -0.02em`).

## 5. Elevação / sombras

| Token | Definição |
| --- | --- |
| Drop Shadow/md | `0 2px 2px #0000000F` + `0 4px 3px #00000012` |
| Drop Shadow/lg | `0 10px 8px #0000000A` + `0 4px 3px #0000001A` |
| Drop Shadow/2xl (dark) | `0 25px 25px #00000026` |

## 6. Assets exportados

> Ícones, imagens, logos e fontes baixados do Figma (`download_assets`) em lotes manuais por seção/frame, seguindo o critério e a convenção de `docs/agent-rules/designer.md` (seção "3.1 Extrair assets"). Salvos em `angular-app/public/assets/<categoria>/` (ex.: `angular-app/public/assets/icons/`, `angular-app/public/assets/images/`). **Correção (07/09/2026):** esta seção e a tabela abaixo diziam `src/assets/<categoria>/` — isso nunca existiu no projeto Angular; os arquivos foram copiados para `angular-app/public/assets/` já em 06/09/2026 (ver log de evolução), mas a convenção documentada aqui só foi corrigida agora. `public/` é a pasta de estáticos do Angular CLI (v17+, confirmado em `angular-app/angular.json`) sob esse projeto — não `src/assets/`.

| Lote/Seção | Asset | Categoria | Node ID (Figma) | Caminho no repo |
| --- | --- | --- | --- | --- |
| Skills | icon-javascript | icons | `316:267` | `angular-app/public/assets/icons/icon-javascript.svg` |
| Skills | icon-typescript | icons | `316:272` | `angular-app/public/assets/icons/icon-typescript.svg` |
| Skills | icon-react | icons | `316:277` | `angular-app/public/assets/icons/icon-react.svg` |
| Skills | icon-nextjs | icons | `316:285` | `angular-app/public/assets/icons/icon-nextjs.svg` |
| Skills | icon-nodejs | icons | `316:295` | `angular-app/public/assets/icons/icon-nodejs.svg` |
| Skills | icon-express | icons | `316:300` | `angular-app/public/assets/icons/icon-express.svg` |
| Skills | icon-nest | icons | `316:304` | `angular-app/public/assets/icons/icon-nest.svg` |
| Skills | icon-socket | icons | `316:308` | `angular-app/public/assets/icons/icon-socket.svg` |
| Skills | icon-postgresql | icons | `316:313` | `angular-app/public/assets/icons/icon-postgresql.svg` |
| Skills | icon-mongodb | icons | `316:316` | `angular-app/public/assets/icons/icon-mongodb.svg` |
| Skills | icon-sass | icons | `316:320` | `angular-app/public/assets/icons/icon-sass.svg` |
| Skills | icon-tailwindcss | icons | `316:324` | `angular-app/public/assets/icons/icon-tailwindcss.svg` |
| Skills | icon-figma | icons | `316:332` | `angular-app/public/assets/icons/icon-figma.svg` |
| Skills | icon-cypress | icons | `316:340` | `angular-app/public/assets/icons/icon-cypress.svg` |
| Skills | icon-storybook | icons | `316:349` | `angular-app/public/assets/icons/icon-storybook.svg` |
| Skills | icon-git | icons | `316:354` | `angular-app/public/assets/icons/icon-git.svg` |
| Hero | icon-pin (localização) | icons | `317:709` | `angular-app/public/assets/icons/icon-pin.svg` |
| Hero / Contact me | icon-social-github | icons | `165:123` (biblioteca "Styles & Components" > "Icons", símbolo `icon-github`, Size=24/Light — node antigo `317:734` estava errado, ver nota abaixo) | `angular-app/public/assets/icons/icon-social-github.svg` |
| Hero / Contact me | icon-social-twitter | icons | `165:117` (idem, símbolo `icon-twitter`, Size=24/Light — node antigo `317:738`) | `angular-app/public/assets/icons/icon-social-twitter.svg` |
| Hero / Contact me | icon-social-figma | icons | `165:121` (idem, símbolo `icon-figma`, Size=24/Light — node antigo `317:742`) | `angular-app/public/assets/icons/icon-social-figma.svg` |
| Contact me | icon-mail | icons | `327:352` | `angular-app/public/assets/icons/icon-mail.svg` |
| Contact me | icon-phone | icons | `327:366` | `angular-app/public/assets/icons/icon-phone.svg` |
| Contact me | icon-copy (botão "copiar", reusado 2x) | icons | `165:109` (biblioteca "Styles & Components" > "Icons", símbolo `icon-copy`, Size=32/Light — node antigo `327:373` estava errado, ver nota abaixo) | `angular-app/public/assets/icons/icon-copy.svg` |
| Footer | icon-footer (junto ao copyright) | icons | `327:412` | `angular-app/public/assets/icons/icon-footer.svg` |
| Hero | image-hero-avatar (foto de perfil, placeholder "Sagar") | images | `316:228` | `angular-app/public/assets/images/image-hero-avatar.png` |
| About | image-about-photo (foto de perfil da seção "Sobre mim") | images | `316:239` | `angular-app/public/assets/images/image-about-photo.png` |
| Work | image-project-thumbnail (thumbnail de projeto, reusado nos 3 cards) | images | `316:425` | `angular-app/public/assets/images/image-project-thumbnail.png` |

> **Biblioteca canônica de ícones descoberta em 07/09/2026:** o arquivo tem uma página "Styles & Components" (node `114:2`, fora da árvore de seções `316:*`/`317:*`/`327:*` usada no resto desta seção) com uma sub-área "Icons" (node `163:5`) contendo todo o conjunto de ícones do design system como símbolos isolados, já recortados no tamanho certo, em 4 variantes cada (Light/Dark × 24px/32px): `icon-github`, `icon-twitter`, `icon-figma`, `icon-inbox` (mail), `icon-copy`, `icon-phone`, `icon-copyright` (footer), `icon-dark-mode`, `icon-light-mode`, `icon-close`, `icon-menu`, `icon-location` (pin), `icon-external-link`. Essa é a fonte que deveria ter sido usada desde o início para todo ícone desta tabela — ela existia durante toda a extração original, mas não foi localizada (ver `docs/agent-rules/designer.md`, seção 3.1, para o processo agora corrigido). `icon-pin`, `icon-mail` e `icon-phone` não estavam aninhados dentro de outro componente na extração original, então saíram corretos por acaso; `icon-social-github`/`icon-social-twitter`/`icon-social-figma`/`icon-copy` estavam aninhados dentro de um `Icon Button` e saíram com o bug descrito na entrada de 07/09/2026 do log de evolução — já corrigidos reexportando desta biblioteca (node IDs atualizados na tabela acima). `icon-footer` (equivalente a `icon-copyright` aqui) e o ícone de tema/menu do Header (pendência da seção 7) não foram reexportados nesta rodada por estarem fora do escopo pedido, mas agora têm node de origem confirmado e podem ser resolvidos sem nova investigação.

> As 3 imagens acima são conteúdo de placeholder do template ("Sagar", projeto fictício) — exportadas só como referência estrutural de proporção (280×320, 400×480, 496×402), não para uso final no site. O thumbnail de projeto (`316:425`) é o mesmo arquivo reaproveitado também nos cards `327:279` e `327:312` — baixado uma única vez. Testimonials não tem foto raster: os avatares dos depoimentos são círculos de cor sólida com um ícone SVG genérico dentro (categoria `icons`, não `images`), não incluídos aqui.
>
> Os 3 `Icon Button` de redes sociais no Hero e no Contact me referenciam a mesma instância genérica de componente (`Icon`) — o ícone real (GitHub/Twitter/Figma) só aparece resolvido no *screenshot*, não no `get_metadata`. Confirmado visualmente via `get_screenshot`; exportado uma única vez por rede (o par idêntico no Hero/Contact não foi duplicado).
>
> **Pendente, mas desbloqueado (07/09/2026):** o ícone do `Icon Button` de tema/menu no Header (node de override `I316:588;316:604`, formato não aceito pelas tools de export) ainda não foi exportado, mas o node "achatado" equivalente foi localizado na biblioteca canônica "Styles & Components" > "Icons" (`163:5`, ver nota logo acima da tabela desta seção): símbolos `icon-light-mode`/`icon-dark-mode`, Size=24, em Light (`165:107`/`165:108`) e Dark (`312:239`/`312:235`). Não exportado nesta rodada por estar fora do escopo pedido (só os 4 ícones sociais/copy) — próxima tarefa que tratar do Header/tema pode exportar direto desses IDs, sem precisar resolver o override de novo.

### 6.1 Assets externos (não vêm do Figma)

> **Origem diferente das tabelas acima:** os assets a seguir **não** foram extraídos do Figma — são ícones de marca baixados do [Devicon](https://github.com/devicons/devicon) (MIT), pra fechar o match de skills reais do Johnny que o template do Figma não cobria (seção 10, "Skills — ícones sem match", decisão revertida em 07/09/2026 a pedido do Johnny). Mantidos separados desta tabela pra não confundir proveniência: qualquer coisa na tabela da seção 6 acima tem um node ID do Figma rastreável; o que está aqui embaixo não tem, vem de um repositório de terceiros (licença MIT, sem necessidade de atribuição por arquivo, mas registrada aqui por rastreabilidade).

| Skill | Asset | Variante Devicon | Caminho no repo |
| --- | --- | --- | --- |
| Spring Framework / Spring Boot | icon-spring | `spring/spring-original.svg` | `angular-app/public/assets/icons/icon-spring.svg` |
| Java | icon-java | `java/java-original.svg` | `angular-app/public/assets/icons/icon-java.svg` |
| HTML5 | icon-html5 | `html5/html5-original.svg` | `angular-app/public/assets/icons/icon-html5.svg` |
| CSS3 | icon-css3 | `css3/css3-original.svg` | `angular-app/public/assets/icons/icon-css3.svg` |
| Angular (o real do currículo é AngularJS, não o Angular atual do próprio site) | icon-angularjs | `angularjs/angularjs-original.svg` | `angular-app/public/assets/icons/icon-angularjs.svg` |
| Grunt | icon-grunt | `grunt/grunt-original.svg` | `angular-app/public/assets/icons/icon-grunt.svg` |
| GitHub | icon-github | `github/github-original.svg` | `angular-app/public/assets/icons/icon-github.svg` |
| DynamoDB | icon-dynamodb | `dynamodb/dynamodb-original.svg` | `angular-app/public/assets/icons/icon-dynamodb.svg` |
| AWS | icon-aws | `amazonwebservices/amazonwebservices-plain-wordmark.svg` | `angular-app/public/assets/icons/icon-aws.svg` |
| Azure | icon-azure | `azure/azure-original.svg` | `angular-app/public/assets/icons/icon-azure.svg` |
| VS Code | icon-vscode | `vscode/vscode-original.svg` | `angular-app/public/assets/icons/icon-vscode.svg` |

> Todos baixados na variante `original` (128×128, colorido, sem fundo), exceto AWS — só existe como wordmark no Devicon (ver nota abaixo) — pra manter consistência visual com os ícones de tecnologia já existentes do Figma (`icon-javascript`, `icon-nodejs`, `icon-git`, `icon-sass`, `icon-typescript`, `icon-postgresql`, `icon-mongodb` — seção 6, essas 3 últimas já estavam no template original do Figma e passaram a ter match real depois da revisão de 07/09/2026 da lista de skills), que também são marcas coloridas sem fundo quadrado (exceto `icon-javascript`, que já tinha um fundo amarelo no próprio template do Figma).
>
> **AWS não tem ícone "só símbolo" no Devicon** — só existe como wordmark (`amazonwebservices-line-wordmark.svg`, `-original-wordmark.svg`, `-plain-wordmark.svg`, confirmado direto na pasta do repositório via GitHub API, não só no índice `devicon.json`) — a marca "aws" sempre inclui o texto + a seta/smile laranja, formato retangular largo em vez de quadrado. Decisão do Johnny: usar mesmo assim a variante `plain-wordmark` (a fonte tem `viewBox` quadrado 128×128 com o desenho centralizado, então não precisa de `iconWidth` customizado no componente — cabe bem no slot de 64×64 igual aos outros ícones, só com mais espaço vazio nas laterais).
>
> **LESS, Oracle Database, Microsoft SQL Server e jQuery não fazem mais parte da lista de skills** (removidos pelo Johnny na revisão de 07/09/2026, ao trocar a lista original por um conjunto atualizado de tecnologias reais) — os arquivos `icon-oracle.svg`/`icon-sqlserver.svg`/`icon-jquery.svg`, que tinham sido baixados do Devicon numa rodada anterior desta mesma tarefa, foram removidos do repo por ficarem órfãos (não referenciados em lugar nenhum). LESS nunca chegou a ter arquivo baixado — decisão anterior já tinha sido trocar por Sass/Scss (`icon-sass.svg`, já existente do Figma) por causa da mesma limitação de "só wordmark" no Devicon.

## 7. Pendências para a Fase 2 (não cobertas neste extract)

- [x] Especificações de componentes individuais (Header, Icon Button, Tag, cards de projeto, timeline) — extraídas em 06/09/2026 via `get_design_context`, ver seção 8. Cobertura: Header, Footer, Icon Button (variantes 36×36 e 44×44), Tag, card de projeto (Work, 3 cards), item de timeline (Experience, 3 itens) e card de depoimento (Testimonials). **Ressalva não bloqueante:** o Icon Button expõe uma prop `state` no componente (valor visto: `Default`), mas não foi possível confirmar variantes `Hover`/`Active` como frames estáticos no arquivo — ver seção 8 para detalhe. Isso não impede a implementação (a variante visível/`Default` está totalmente documentada); só significa que hover/active precisarão ser definidos por convenção do projeto, não copiados do Figma.
- [x] Ícones de tecnologia (Skills), sociais (Hero/Contact me) e estruturais (pin, mail, phone, copy, footer) — extraídos em 06/09/2026, ver tabela da seção 6; 4 deles (github/twitter/figma/copy) reexportados em 07/09/2026 da biblioteca canônica "Styles & Components" > "Icons" (`163:5`) para corrigir um bug de recorte (ver log de evolução). Falta só o ícone de tema/menu do Header — não é mais bloqueado por ID de instância, o node equivalente já foi localizado na mesma biblioteca (ver nota da seção 6), só falta exportar.
- [x] Imagens raster estruturais (avatar do Hero, foto do About, thumbnail de projeto do Work) — extraídas em 06/09/2026, ver tabela da seção 6. São placeholders do template, só pra referência de proporção.
- [x] Decidir com o Johnny se **dark mode** e o **menu mobile dedicado** entram no escopo desta migração — **resolvido:** ambos entram no escopo. Decisão já estava registrada em `docs/PLANO-MIGRACAO-ANGULAR.md` (seção "2. Decisões arquiteturais já tomadas" e "8. Log de evolução deste plano", entrada de 06/09/2026: "Dark mode e menu mobile dedicado adicionados ao escopo da migração (Johnny confirmou)") — este item aqui só estava desatualizado em relação a esse registro; Johnny reconfirmou agora (06/09/2026).
- [x] Escopo de **tablet** decidido — **resolvido:** tablet também entra no escopo da migração (Johnny confirmou em 06/09/2026), mesmo o Figma não tendo nenhuma referência visual para essa faixa. Reconfirmado agora, via `get_screenshot` nos grupos `328:4104` (Light) e `328:4105` (Dark) da seção 1, que o arquivo continua tendo **apenas duas larguras de frame**: Desktop (1440px) e Mobile/iPhone 8 (375px, repetida no frame de "Mobile Menu") — nenhuma largura intermediária de tablet existe no Figma, em nenhum dos dois temas. Como não há dado extraível, o breakpoint e o layout intermediário abaixo são **convenção de engenharia proposta**, não extração:
  - [ ] **Breakpoint sugerido (não é valor visto no Figma):** tablet como faixa intermediária entre os dois extremos confirmados (Mobile 375 / Desktop 1440) — proposta: **~768px a ~1023px**, com desktop assumido a partir de **1024px**. É a convenção de mercado amplamente usada (ex.: breakpoints `md`/`lg` do Tailwind CSS), não um dado do arquivo Figma. A validar com o Johnny/na implementação.
  - [ ] **Layout intermediário (768–1023px) não pode ser copiado do Figma** — precisa ser interpolado/desenhado na Fase 2 de implementação (`docs/PLANO-MIGRACAO-ANGULAR.md`), usando como base os paddings/gaps já documentados na seção 2 (ex.: reduzir progressivamente o padding horizontal de seção de 80px, no desktop, até o valor usado no mobile). **Atualização (06/09/2026):** as duas pontas da interpolação agora estão confirmadas — desktop em **80px horizontal / 96px vertical** (seção 2, "Padding de seção") e mobile em **16px horizontal / 64px vertical** (seção 2, "Padding de seção — Mobile", amostra Hero/About/Skills/Work no frame `327:417`). Isso resolve a lacuna anterior (mobile não tinha padding lateral medido). A ressalva permanece: o breakpoint (~768–1023px) e o layout de tablet em si continuam sendo **convenção de engenharia proposta**, não uma extração do Figma — o arquivo não tem nenhum frame nessa largura (ver início deste item); só as duas pontas do intervalo (mobile/desktop) que servem de base para a interpolação é que agora estão medidas.

## 8. Especificações de componentes

> Extraído via `get_design_context` (Desktop/Light) em 06/09/2026. Cores, tipografia e espaçamento abaixo referenciam os tokens já documentados nas seções 3/4/2 — não há valor solto novo introduzido aqui. Screenshots de conferência gerados via `get_screenshot` durante a extração (URLs de curta duração, não reproduzidas aqui).

### 8.1 Header (`316:588`)

- **Dimensões:** 1440×68 (largura total, altura fixa). Nomeado/assumido como sticky na seção 1 (altura fixa de barra), mas **não foi possível confirmar comportamento sticky/scroll**: o container tem uma borda inferior (`border-bottom`) na cor `rgba(255,255,255,0)` (transparente) — compatível com um padrão comum de "borda que aparece ao rolar a página", mas o arquivo não tem um frame/estado separado de "Header rolado" nem uma interação de prototype inspecionável pelas tools disponíveis. Tratar como suposição de implementação, não como spec confirmada.
- **Cor de fundo:** `--color-gray-default` (branco).
- **Padding:** 80px horizontal / 16px vertical (seção 2), mais 32px horizontal do `Container` interno.
- **Estrutura (esquerda → direita):**
  - Logo/nome `<SS />`: texto Bold 30px/36px, cor `--color-gray-900`, tracking -0.6px (ver nota de unidade na seção 4) — **não corresponde a nenhum token nomeado da tabela da seção 4** (mistura peso Bold com tamanho de H3); tratar como estilo ad-hoc do template, a substituir pelo nome/logo real do Johnny.
  - Navegação (`gap: 24px`): 4 links de texto (About, Work, Testimonials, Contact) — estilo **Body2/Medium**, cor `--color-gray-600`.
  - Divider vertical entre navegação e ações (linha simples, sem token de cor específico além do stroke padrão).
  - Bloco "Actions" (`gap: 16px`): Icon Button (tema/menu, ver 8.3) + Botão "Download CV" (fundo `--color-gray-900`, texto `--color-gray-50`, estilo Body2/Medium, `padding: 6px 16px`, `border-radius: 12px`).
- **Estados:** nenhum estado de hover/active foi encontrado como frame estático para os links de navegação ou para o botão "Download CV" — não confirmável via Figma; convenção de hover deve ser decidida na implementação.

### 8.2 Footer (`316:579`)

- **Dimensões:** 1440×68 (largura total, altura fixa).
- **Cor de fundo:** `--color-gray-50` (diferente do Header, que é `--color-gray-default`).
- **Padding:** 80px horizontal / 24px vertical (seção 2), mais 32px horizontal do `Container` interno.
- **Estrutura:** um único bloco centralizado "Footer Note" com `gap: 8px` — ícone de copyright (16×16, asset `icon-footer`, já exportado na seção 6) + texto de copyright em **Body3/Normal** (14px/20px), cor `--color-gray-600`, com dois trechos em link sublinhado ("Designed" e "coded", placeholders do template a substituir pelos créditos reais).
- **Estados:** não aplicável (sem elementos interativos além dos 2 links de texto, sem hover confirmável).

### 8.3 Icon Button

Componente reusado no Header (tema/menu), Hero e Contact me (redes sociais), e Work/Contact me (ações). Duas variantes de tamanho observadas — **não há uma prop `size` com nomes tipo "sm/md/lg" confirmada por múltiplos valores; o único valor de `size` visto explicitamente foi `"md"`** (no Header), mas o mesmo componente aparece com dimensão total diferente em Contact me:

| Variante | Dimensão total | Padding | Ícone interno | Onde aparece | Node de referência |
| --- | --- | --- | --- | --- | --- |
| Padrão (36×36) | 36×36 | 6px (`--space-icon-button-padding`) | 24×24 | Header (tema/menu), Hero (social ×3), Work (ação do card), Contact me (social ×3) | `I316:588;316:604` (Header), `317:734` (Hero) |
| Grande (44×44) | 44×44 | 6px | 32×32 | Contact me (botão "copiar" ao lado de e-mail/telefone) | `327:373`, `327:377` |

- **Border-radius:** 8px em ambas as variantes.
- **Cor de fundo:** transparente (sem `background` aplicado) em todas as instâncias inspecionadas — o botão é só o ícone com padding, sem fundo/borda visível no estado default.
- **Ícone:** SVG interno, cor herdada do ícone exportado (ver seção 6 para os ícones sociais/estruturais já baixados); o ícone de tema/menu do Header (formato de "sol", conforme screenshot) **segue pendente de exportação como asset isolado** (mesmo bloqueio já registrado na seção 6 — node de override `I316:588;316:604;309:256` não aceito por `download_assets`), mas sua estrutura de layout (padding/tamanho) **foi** confirmada aqui via `get_design_context`, que não tem essa mesma restrição de formato de node ID.
- **Variantes/props do componente:** a instância do Header expõe explicitamente as props `size="md"`, `state="Default"`, `themeMode="Light"` na assinatura gerada pelo `get_design_context` — ou seja, o componente principal tem variantes de `state` e `themeMode` no Figma, mas **não foi possível enumerar quais outros valores essas props aceitam** (ex.: `state="Hover"`/`"Active"`) porque o arquivo não expõe Code Connect nem uma página de biblioteca de componentes separada (`list_file_components_for_code_connect` retornou erro de permissão — recurso exige seat Dev/Full em plano Organization/Enterprise) e nenhum outro frame estático com `state` diferente de `Default` foi localizado. **Não adivinhado:** hover/active do Icon Button não são especificados aqui — devem ser definidos por convenção do projeto na implementação.

### 8.4 Tag

Badge usado em About, Skills, Work, Experience, Testimonials e Contact me (título de seção) — mesmo componente em todos os usos, sem variante de cor observada.

- **Dimensão:** auto (largura ajustada ao texto), altura efetiva 28px (`padding-y` 4px × 2 + linha de 20px do texto).
- **Padding:** `20px` horizontal / `4px` vertical (`--space-tag-padding-x` / `--space-tag-padding-y`).
- **Cor de fundo:** `--color-gray-200`.
- **Cor de texto:** `--color-gray-600`.
- **Tipografia:** `Body3/Medium` (14px/20px, peso 500).
- **Border-radius:** 12px.
- **Estados:** não há Tag interativo no arquivo (é só rótulo/badge) — sem hover/active a documentar.

### 8.5 Card de projeto (Work, `316:415`)

3 cards confirmados (Row `316:423`, `327:277`, `327:310`), todos com a mesma estrutura, alternando o lado da imagem (card 1: imagem à esquerda / texto à direita; card 2: texto à esquerda / imagem à direita; card 3: imagem à esquerda / texto à direita de novo — não é uma alternância estrita a cada card, é o padrão observado nos 3 existentes).

- **Dimensão:** 1152px de largura (dentro do container de 1280px com `padding-x: 64px` — nota: esse é um inset diferente do "32px" padrão de outras seções, específico do Work, já registrado na seção 2 como a exceção sem o padding extra de Container), altura 480px por card.
- **Border-radius:** 12px (card inteiro e a imagem interna).
- **Sombra:** `--shadow-md` no card inteiro; a imagem interna (`Picture`) tem sua própria sombra, mais pronunciada — `--shadow-lg`.
- **Cor de fundo:** lado da imagem usa `--color-gray-50` com borda `--color-gray-100`; lado do texto usa `--color-gray-default` (branco).
- **Padding interno:** 48px em ambas as colunas (`--space-card-padding`), confirmando o valor já registrado na seção 2.
- **Estrutura (lado de texto):** título do projeto (`Body1`/Subtitle Semi Bold 20px/28px, cor `--color-gray-900`) → descrição (`Body2/Normal`, 16px/24px, `--color-gray-600`) → grid de Tags de tecnologia (`gap: 8px`, wrap) → bloco "Actions" com 1 Icon Button (variante 36×36, link externo do projeto) — `gap: 24px` entre esses 4 blocos.
- **Estados:** sem hover/active confirmável nos cards nem no Icon Button de ação (mesma ressalva da seção 8.3).

### 8.6 Item de timeline (Experience, `316:357`)

3 itens confirmados (Row `316:365`, `316:378`, `316:405`), mesma estrutura, alturas diferentes por causa da quantidade de linhas de descrição (bullet list).

- **Dimensão:** largura 896px (dentro do container de 1280px, mais estreito que as outras seções — inset de `192px` de cada lado, não os 32px padrão do Container), altura auto (varia com o conteúdo: 288px, 264px, 180px nos 3 itens observados).
- **Border-radius:** 12px.
- **Sombra:** `--shadow-md` (mesma sombra do card do Work).
- **Cor de fundo:** `--color-gray-default` (branco).
- **Padding interno:** 32px nas 4 bordas (`--space-timeline-item-padding`), confirmando o valor já registrado na seção 2.
- **Estrutura interna (`gap: 48px` entre colunas):** logo da empresa (imagem/SVG, ex. "logo-upwork" 102×28) → coluna de conteúdo (cargo em `Subtitle/Semi Bold` 20px/28px cor `--color-gray-900`, lista de bullets em `Body2/Normal` 16px/24px cor `--color-gray-600`, `gap: 16px` entre cargo e lista) → período (`Body2/Normal` 16px/24px, cor `--color-gray-700` — mais escuro que a descrição, mesmo estilo de tamanho).
- **Conector visual da timeline:** **não encontrado.** Inspecionado via `get_screenshot` na seção inteira (`316:357`) — os 3 itens são simplesmente cards empilhados com `gap: 48px` entre si (mesmo valor "gap macro" já documentado na seção 2), sem linha vertical, marcador/dot ou qualquer elemento gráfico de timeline conectando os cards. Se uma timeline visual (linha + marcadores) for desejada na implementação Angular, é uma decisão de UI nova, não uma reprodução do Figma.
- **Estados:** não aplicável (itens não são interativos no Figma).

### 8.7 Card de depoimento (Testimonials, `316:510`)

3 cards confirmados (Column `316:519`, `316:525`, `316:531`), mesma estrutura, dispostos lado a lado (não empilhados).

- **Dimensão:** ~373px de largura cada (3 colunas dividindo 1216px com `gap` implícito da distribuição), altura auto (varia com o tamanho do depoimento: 428px nos dois primeiros, mais alto no terceiro por ter texto mais longo).
- **Border-radius:** 12px.
- **Sombra:** `--shadow-md`.
- **Cor de fundo:** `--color-gray-default` (branco).
- **Padding interno:** 48px nas 4 bordas (`--space-card-padding`), confirmando o valor já registrado na seção 2.
- **Estrutura (`gap: 24px` entre blocos):** avatar circular 64×64 (fundo sólido cinza `#9ca3af` — cor de preenchimento direta, **não corresponde a nenhum token cinza da seção 3**, provável placeholder do template — com um ícone de usuário genérico 40×40 dentro, `padding: 20px`) → texto do depoimento em `Body2/Normal` (16px/24px, `--color-gray-600`) → bloco "Customer Details" (`gap: 4px`): nome em `Subtitle/Semi Bold` (20px/28px, `--color-gray-900`) + cargo/empresa em `Body3/Normal` (14px/20px, `--color-gray-600`).
- **Nota:** confirma o que já constava na seção 6 — Testimonials não usa foto raster, o avatar é um círculo de cor sólida com ícone SVG genérico dentro.
- **Estados:** não aplicável (cards não são interativos no Figma).

### 8.8 Hero (`316:194`)

> Extraído via `get_design_context` em 06/09/2026 (Desktop/Light) — resolve o item 1 da rodada de specs finas pedida antes da geração dos componentes Angular.

- **Dimensão:** 1440×552 (largura total, altura conforme conteúdo). **Cor de fundo:** `--color-gray-default` (branco). **Padding:** 80px horizontal / 96px vertical (seção 2), mais 32px horizontal do `Container` interno.
- **Composição geral:** duas colunas lado a lado dentro do `Container` (`gap: 48px`, `flex-wrap`), altura da linha 360px:
  - **Coluna esquerda** (`max-width: 768px`, `flex: 1`, `self-stretch`, conteúdo centralizado verticalmente via `justify-content: center`): empilha 3 blocos com `gap: 48px` entre eles — `Content`, o grupo `Location + Hire`, e `Actions`.
    - `Content` (`gap: 8px` vertical): título **Heading/H1/Bold** (60px/72px, tracking -1,2px — seção 4) cor `--color-gray-900` (`Hi, I'm Sagar 👋`, placeholder) → parágrafo de bio **Body2/Normal** (16px/24px) cor `--color-gray-600`, largura 768px.
    - Grupo `Location + Hire` (`gap: 8px` vertical entre as 2 linhas): linha "Location" (`gap: 8px` horizontal) com ícone `icon-pin` 24×24 (seção 6) + texto **Body2/Normal** cor `--color-gray-600`; linha "Hire"/disponibilidade (`gap: 8px` horizontal) com um dot verde 8×8 (cor sólida `#10b981`, **não é token da seção 3** — valor "solto" tipo Tailwind `emerald-500`) dentro de uma caixa 24×24, + texto **Body2/Normal** cor `--color-gray-600` ("Available for new projects").
    - `Actions` → `Links` (`gap: 4px` horizontal, seção 2): 3 `Icon Button` (variante 36×36, seção 8.3) — GitHub, Twitter/X e Figma, os mesmos 3 ícones sociais já catalogados na seção 6.
  - **Coluna direita** (`flex: 1`, `min-width: 384px`, `self-stretch`, itens alinhados à direita e centralizados verticalmente): `Pic Container` — composição em camadas (mesmo padrão visto no card do Work/seção 8.5, mas aplicado à foto): um retângulo `Background` 280×320 (cor `--color-gray-200`, borda 8px `--color-gray-default`) deslocado 40px para baixo-direita **atrás** da foto `Pic` (280×320, mesma borda 8px branca) — efeito de "cartão em camadas", não uma foto solta.
- **Tipografia por linha (referência seção 4):** nome/saudação → Heading/H1/Bold; bio, localização e disponibilidade → Body2/Normal; sem uso de Subtitle nesta seção.
- **Estados:** nenhum estado interativo confirmável (mesma ressalva dos Icon Button, seção 8.3).

### 8.9 About (`316:229`)

> Resolve o item 2 da rodada de specs finas — estrutura da "checklist"/quick bits e posição foto/bio.

- **Dimensão:** 1440×962. **Cor de fundo:** `--color-gray-50`. **Padding:** 80px horizontal / 96px vertical (seção 2), mais 32px horizontal do `Container` interno. `Container` com `gap: 48px` vertical, itens centralizados horizontalmente.
- **Row 1:** `Tag` (seção 8.4) centralizado — "About me".
- **Row 2** (`gap: 48px`, `flex-wrap`, altura fixa 694px):
  - **Coluna esquerda** (`flex: 1`, `min-width: 444px`): `Pic Container` — mesma composição em camadas do Hero (8.8), mas espelhada: `Background` 400×480 (`--color-gray-200`, borda 8px `--color-gray-50`) deslocado para **baixo-esquerda**, atrás da `Pic` (400×480, mesma borda) que fica deslocada para **cima-direita** — inverso do deslocamento do Hero (lá o fundo ficava atrás à direita/baixo da foto).
  - **Coluna direita** (`flex: 1`, `min-width: 444px`, `gap: 24px` vertical): título **Heading/H3/Semi Bold** (30px/36px, tracking -0,6px) cor `--color-gray-900` ("Curious about me? Here you have it:") → bloco `Content` (`gap: 16px` vertical, texto **Body2/Normal** 16px/24px cor `--color-gray-600`) com 4 parágrafos de bio (2 deles com trechos em link sublinhado inline) → parágrafo curto de transição ("Finally, some quick bits about me.") → `Checklist` → parágrafo de fechamento.
  - **Checklist (`316:248`):** 2 colunas (`flex: 1` cada) com `gap: 10px` **horizontal** entre elas; dentro de cada coluna, `gap: 10px` **vertical** entre 2 itens de lista (`<ul><li>`, bullet disco, indent 24px, mesmo estilo **Body2/Normal** do texto ao redor) — **4 itens no total** (2 por coluna: ex. "B.E. in Computer Engineering"/"Full time freelancer" na primeira, "Avid learner"/"Aspiring indie hacker" na segunda — conteúdo placeholder). O valor **10px** (já registrado na tabela "Espaçamento observado" da seção 2) é reaproveitado nos dois eixos — gap entre colunas e gap entre itens dentro da coluna —, não é um valor exclusivo de um dos dois usos.
- **Posição foto/bio:** foto sempre à **esquerda**, bloco de texto (heading + bio + checklist) sempre à **direita** — layout fixo, sem alternância de lado (diferente do card do Work, seção 8.5, que alterna).

### 8.10 Skills (`316:256`)

> Resolve o item 3 da rodada de specs finas — organização do grid de ícones e comportamento com item texto-only (skills reais sem ícone, seção 10).

- **Dimensão:** 1440×560. **Cor de fundo:** `--color-gray-default` (branco). **Padding:** 80px horizontal / 96px vertical (seção 2), mais 32px horizontal do `Container` interno.
- **Row 1** (`gap: 16px` vertical): `Tag` (seção 8.4) centralizado "Skills" → subtítulo **Subtitle/Normal** (20px/28px) cor `--color-gray-600`, centralizado, `max-width: 576px` ("The skills, tools and technologies I am really good at:").
- **Grid de ícones:** 2 linhas (`Row`) com `gap: 48px` vertical entre elas, **8 itens por linha** (16 no total no Figma — seção 10 documenta que só 3 têm match com skills reais do Johnny: JS/Node/Git). Cada linha usa `display: flex; align-items: center; justify-content: space-between` — **não é um CSS grid de colunas fixas com gap nomeado**: é uma linha flex que distribui os 8 itens uniformemente pela largura total (1216px) via `justify-between`, então o espaçamento horizontal entre itens varia com a largura de cada ícone (não há um "gap" único). Altura da linha: 100px (linha 1) / 96px (linha 2) — a diferença vem só da altura real dos ícones renderizados, não de um valor de design distinto.
  - Cada item (`Tech`): `flex-col`, `gap: 8px`, `items-center`, altura = altura da linha (`h-full`), `justify-content: center` (centraliza verticalmente o conjunto ícone+label dentro da altura fixa da linha).
    - Ícone: caixa de **64px de altura** (largura variável conforme a proporção real do ícone — ex. React 69,8px, Node.js 56,3px — não é uma caixa quadrada fixa em todos os casos).
    - Label: **Body1/Normal** (18px/28px) cor `--color-gray-600`, `whitespace-nowrap`.
- **Achado sobre item misto (sem ícone) — resolve a dúvida da seção 10:** o Figma **não tem uma variante "texto-only" do item `Tech`** — todo item existente no arquivo tem os 2 elementos (ícone 64px + label), e o alinhamento vertical do conjunto depende do `justify-content: center` do item dentro da altura fixa da linha (100px/96px). Se um item real ficar só com o label (sem o bloco de ícone, como é o caso da maioria dos skills reais do Johnny), o `justify-center` recentraliza o texto sozinho no meio da altura da linha — o que **desalinha esse label** em relação aos labels dos itens vizinhos que têm ícone (cujo label fica "empurrado" para baixo pelo espaço do ícone acima, resultado do `gap: 8px` + altura do ícone). Ou seja: **o grid não é robusto de fábrica para misturar, lado a lado na mesma linha, itens com e sem ícone** — isso não é adivinhação de conteúdo, é uma limitação estrutural do Auto Layout encontrada nesta extração. Para a Fase 2 (onde a maioria dos skills reais é texto-only, seção 10), duas saídas possíveis, a decidir na implementação (não no Figma): **(a)** reservar um espaço vazio do tamanho do ícone (64px) mesmo nos itens sem ícone, preservando `justify-center`/`gap: 8px`, para os labels ficarem alinhados horizontalmente entre si; ou **(b)** trocar o `justify-center` do item por um alinhamento fixo (ex. `align-items: flex-end`, ou duas linhas fixas — uma para o "slot" do ícone, vazia quando não houver, e outra para o label).

### 8.11 Experience — raiz da seção (`316:357`)

> Resolve o item 4 da rodada de specs finas — arranjo do heading (Tag+H2 [na prática Subtitle, ver abaixo]) acima da lista de itens já documentados na seção 8.6.

- **Dimensão:** 1440×1140. **Cor de fundo:** `--color-gray-50`. **Padding:** 80px horizontal / 96px vertical (seção 2), mais 32px horizontal do `Container` interno. `Container` (`316:358`) com `gap: 48px` vertical, itens centralizados horizontal e verticalmente.
- **Bloco de heading** (`316:359`, `gap: 16px` vertical): `Tag` (seção 8.4) centralizado "Experience" → subtítulo **Subtitle/Normal** (20px/28px) cor `--color-gray-600`, centralizado, `max-width: 576px` ("Here is a quick summary of my most recent experiences:") — **nota:** apesar do pedido descrever "Tag+H2", o texto abaixo do Tag usa o token **Subtitle/Normal**, não um **Heading/H2** — mesmo padrão de heading já visto em Skills (8.10) e Contact me (8.12): Tag + Subtitle, não Tag + H2, em todas as seções de conteúdo com esse padrão de cabeçalho centralizado (única exceção real a H2 no arquivo é o "email" do Contact me, que usa Heading/H2 por outro motivo — ver 8.12).
- **Espaçamento entre o heading e os itens:** confirmado via geometria (`get_metadata`) que o **mesmo valor de gap 48px** do `Container` rege tanto a distância entre o bloco de heading e o primeiro item quanto a distância entre cada item e o próximo — não há um valor especial "de lista": item 1 começa em `y=120` (heading termina em `y=72`, +48 = 120); item 2 em `y=456` (item 1 termina em `120+288=408`, +48 = 456); item 3 em `y=768` (item 2 termina em `456+264=720`, +48 = 768). Ou seja, a seção inteira (heading + 3 itens da seção 8.6) é uma única coluna com um único `gap: 48px` uniforme — o "gap macro" já catalogado na seção 2.
- Reconfirma o já registrado na seção 8.6: os 3 itens são simplesmente empilhados, sem conector visual de timeline entre eles.

### 8.12 Contact me (`316:537`)

> Resolve o item 5 da rodada de specs finas — arranjo geral (heading, linhas de email/phone, bloco de social links), colunas ou empilhado, alinhamento. Aproveitado também para checar a pendência de ícone de LinkedIn (ver seção 10).

- **Dimensão:** 1440×560. **Cor de fundo:** `--color-gray-default` (branco). **Padding:** 80px horizontal / 96px vertical (seção 2), mais 32px horizontal do `Container` interno. `Container` com `gap: 48px` vertical, itens centralizados horizontal e verticalmente.
- **Bloco de heading** (`gap: 16px` vertical): `Tag` centralizado "Get in touch" → subtítulo **Subtitle/Normal** (20px/28px) cor `--color-gray-600`, centralizado, `max-width: 576px`.
- **Coluna central** (`316:547`, `gap: 16px` vertical, itens centralizados): email e telefone **empilhados** (não lado a lado):
  - **Email** (`gap: 20px` horizontal, itens centralizados): ícone `icon-mail` 32×32 (seção 6) + texto **Heading/H2/Semi Bold** (36px/40px, tracking -0,72px — seção 4) cor `--color-gray-900` (placeholder "reachsagarshah@gmail.com") + `Icon Button` 44×44 (variante grande, seção 8.3) de "copiar".
  - **Phone**: mesma estrutura (`gap: 20px`) — ícone `icon-phone` 32×32 + mesmo estilo **Heading/H2/Semi Bold** + `Icon Button` 44×44 de "copiar".
- **Bloco Social** (`316:564`, centralizado abaixo do bloco email/telefone, `gap: 8px` vertical): texto **Body2/Normal** (16px/24px) cor `--color-gray-600` ("You may also find me on these platforms!") → `Links` (`gap: 4px` horizontal, seção 2) com 3 `Icon Button` 36×36 — os mesmos 3 ícones sociais do Hero (GitHub/Twitter/Figma, seção 6).
- **Arranjo geral / alinhamento:** tudo em **uma única coluna central** (heading → email → telefone → social), alinhamento horizontal **centralizado** em todos os níveis — não há divisão em colunas lado a lado nesta seção (diferente do Header, que é uma linha horizontal).
- **Checagem de ícone de LinkedIn (pendência da seção 10):** verificado nesta rodada, sem extração nova isolada — `search_design_system` (`query: "LinkedIn"`) no arquivo inteiro retornou vazio (nenhum componente/variável/estilo publicado com esse nome), e `get_metadata` no frame completo `316:177` (Home / Desktop / Light — as 9 seções inteiras, não só o Contact me) não encontrou nenhum nó nomeado "linkedin" em lugar nenhum do canvas (os únicos ícones sociais existentes, em qualquer seção, são os já catalogados na seção 6: GitHub, Twitter/X e Figma — sempre os mesmos 3, reaproveitados no Hero e aqui). `get_context_for_code_connect` no componente `Icon` (para tentar enumerar todos os valores possíveis da prop `name`, o que revelaria variantes não usadas no canvas) segue bloqueado pela mesma restrição de plano já registrada nas seções 8.3/9.3. **Conclusão:** dentro do que é possível checar sem acesso Code Connect, **não existe nenhum ícone de LinkedIn no arquivo** (nem usado, nem aparentemente não utilizado) — a decisão já registrada na seção 10 (LinkedIn como texto-only) fica confirmada, não havia asset "escondido" para aproveitar.

### 8.13 Menu mobile (overlay) — `327:1337` (Light) / `327:3601` (Dark)

> Resolve o item 6 da rodada de specs finas. Os node IDs `327:1337`/`327:3601` (já citados na seção 1) são os **frames de referência no canvas**, que contêm a instância do overlay em si **mais** o restante da página mobile embaixo, só como contexto visual (não faz parte do overlay). A especificação abaixo é da instância do componente "Mobile Menu" propriamente dito: `327:1772` (Light) / `327:4036` (Dark), extraída via `get_design_context` isolando essa instância (evita reprocessar a página mobile inteira, ~9400px de altura, por seção).

- **Viewport:** 375×667 (mesmo frame de referência mobile "iPhone 8" da seção 1).
- **Camada de fundo (scrim):** cobre o viewport inteiro — `backdrop-filter: blur(4px)` + cor de fundo semitransparente. Light: `--color-gray-900` a 10% de opacidade (`rgba(17,24,39,0.1)`); Dark: `--color-gray-dark-900` a 10% de opacidade (`rgba(249,250,251,0.1)`). **Nota:** essa variação de opacidade não é um token formal da seção 3 (que só documenta as cores sólidas) — é uma aplicação ad-hoc de opacidade sobre o token de cor 900 já existente em cada tema.
- **Painel do menu ("Menu"):** `position: absolute`, `left: 55px`, `top: 0`, `width: 320px`, `height: 667px` (altura total do viewport). Como `55 + 320 = 375` (largura total do viewport), o painel fica **ancorado à direita**, funcionando como uma gaveta ("drawer") que ocupa ~85% da largura da tela, deixando uma faixa de 55px do scrim visível à esquerda. Sombra `--shadow-2xl` (seção 5: `0 25px 25px rgba(0,0,0,0.15)`). Cor de fundo: `--color-gray-default` (branco) no Light; `--color-gray-dark-default` (`#030712`) no Dark.
- **Estrutura interna do painel** (topo → base, 3 blocos empilhados sem gap entre si — cada bloco delimitado pelo próprio padding/borda):
  1. **Logo** (padding 16px, borda inferior 1px sólida `--color-gray-100`/`--color-gray-dark-100`, `justify-content: space-between`): texto `<SS />` (mesmo texto ad-hoc do logo do Header, seção 8.1) — aqui com um token tipográfico próprio de mobile, **Heading/H3/Bold - Tablet & Mobile** (24px/32px, tracking -0,48px — não estava na tabela da seção 4, que só cobria os estilos vistos no desktop; mesmo peso Bold do H3 desktop, tamanho/line-height ajustados para mobile), cor `--color-gray-900`/`--color-gray-dark-900`; à direita, `Icon Button` 36×36 (seção 8.3) com ícone de fechar ("X").
  2. **Links** (padding 16px, borda inferior 1px sólida `--color-gray-100`/`--color-gray-dark-100`, `flex-col`, `gap: 16px` vertical): 4 links de navegação, nesta ordem — **About → Work → Testimonials → Contact** (mesmos rótulos/ordem da navegação do Header desktop, seção 8.1). Estilo **Body2/Medium** (16px/24px), cor `--color-gray-600`/`--color-gray-dark-600`.
  3. **Actions** (padding 16px, `flex-col`, `gap: 16px` vertical):
     - Linha "Switch Theme" (`justify-content: space-between`): texto "Switch Theme" em **Body2/Normal** (16px/24px) cor `--color-gray-600`/`--color-gray-dark-600` + `Icon Button` 36×36 com ícone de tema — **o ícone muda conforme o tema atual** (sol no Light, sugerindo "mudar para escuro"; lua com um brilho no Dark, sugerindo "mudar para claro") — mesma família de ícone de tema/menu do Header que segue pendente de exportação isolada como asset (seções 6/8.3), aqui confirmada visualmente em 2 variantes (Light/Dark) via screenshot.
     - Botão "Download CV" (largura total do painel, `padding: 16px horizontal / 6px vertical`, `border-radius: 12px`) — **mesmo componente/estilo do botão do Header** (seção 8.1): fundo `--color-gray-900`/`--color-gray-dark-900`, texto `--color-gray-50`/`--color-gray-dark-50`, estilo **Body2/Medium**.
- **Como abre/fecha (visual — não é interação real confirmada):** mesma limitação já registrada para o Header sticky e os estados do Icon Button (seções 8.1/8.3) — o Figma só tem o frame estático do estado "aberto", sem frame de "fechado" nem prototype inspecionável pelas tools disponíveis. A composição (painel ancorado à direita cobrindo ~85% da largura, sobre um scrim com blur cobrindo o resto) sugere visualmente um padrão comum de "drawer" que desliza a partir da direita, mas a transição/animação em si é decisão de implementação, não dado extraído. O botão "X" no bloco Logo é, pela posição/função, o elemento de fechar; **não há um ícone de abrir (hambúrguer) dentro deste frame do overlay** — o gatilho de abertura (presumivelmente um `Icon Button` no Header mobile) não foi mapeado nesta extração por estar fora do escopo do pedido (é parte do Header, não do overlay em si).
- **Dark mode:** aplicado por troca de tokens de cor, mesmo padrão de inversão já documentado na seção 3 — fundo do painel `--color-gray-default` → `--color-gray-dark-default`, borda `--color-gray-100` → `--color-gray-dark-100`, textos `--color-gray-900`/`600` → `--color-gray-dark-900`/`600`, botão CTA `--color-gray-900`/`50` → `--color-gray-dark-900`/`50`; mais a troca do ícone de tema (sol ↔ lua) descrita acima. Estrutura, espaçamento e tipografia são **idênticos** entre os dois temas — só cor (e o ícone de tema) mudam.

## 9. Regras de sizing e resize de componentes

> Verificação pontual (06/09/2026), motivada por checagem preventiva antes da Fase 2 — não é extração de componente novo, é uma checagem de um tipo de dado específico sobre os componentes já documentados na seção 8 (Icon Button, Tag, card de projeto).

### 9.1 Variáveis de tamanho nomeadas

**Não existem.** Confirmado via `get_variable_defs` em 2 amostras adicionais (Header `316:588`, card de projeto do Work `316:423`) — mesmo padrão já registrado na seção 2 para espaçamento: os únicos tipos de variável do arquivo são cor (`Gray/*`), tipografia (`Heading/Body/Subtitle*`) e efeito (`Drop Shadow/*`). Nenhuma variável do tipo `Size/*`, `Width/*`, `Icon Size/*` ou equivalente apareceu em nenhuma amostra (nem nas 4 originais da seção 2, nem nestas 2 novas). Confirma que, assim como o espaçamento, os **tamanhos de componente são valores fixos "soltos"** (px hardcoded no próprio frame/Auto Layout), sem token de sizing reutilizável por trás.

### 9.2 Comportamento de resize do Auto Layout (hug / fill / fixed)

Ao contrário do sizing nomeado (ausente), o **modo de resize do Auto Layout está presente e é consistente** — aparece implicitamente no código gerado pelo `get_design_context` (ausência de classe de largura/altura fixa = hug contents; `flex-[1_0_0]`/`self-stretch` = fill container; classe `size-[Npx]`/`h-[Npx]` explícita = fixo):

| Componente | Eixo | Comportamento | Evidência |
| --- | --- | --- | --- |
| Icon Button (ambas variantes, 36×36 e 44×44) | Horizontal e vertical | **Hug contents** — a caixa não tem `width`/`height` fixos; o tamanho final é resultado de `padding: 6px` (fixo) + tamanho do ícone interno (24px ou 32px) | `317:734` (instância Hero): classe gerada é só `p-[6px] ... rounded-[8px]`, sem `size-[Npx]` |
| Tag | Horizontal e vertical | **Hug contents** — largura ajustada ao texto, altura = `padding-y 4px`×2 + `line-height 20px` (28px). Já descrito como "auto" na seção 8.4; aqui confirmado formalmente como modo hug | `325:214` |
| Card de projeto — colunas internas (imagem / texto) | Horizontal | **Fill container** (`flex-[1_0_0]`) — cada coluna ocupa metade do card, dividindo o espaço disponível igualmente | `316:423`, colunas `316:424`/`316:426` |
| Card de projeto — colunas internas | Vertical | **Fill container** (`self-stretch`) — a altura da coluna acompanha a altura do card (fixa em 480px, seção 8.5) | idem |
| Card de projeto — imagem (`Picture`) | Horizontal | **Fill container** (`flex-[1_0_0]`) dentro da coluna da imagem | `316:425` |
| Card de projeto — imagem (`Picture`) | Vertical | **Fixo** — `h-[384px]` explícito, não acompanha a altura da coluna | `316:425` |
| Card de projeto — card inteiro (`Row`) | Horizontal e vertical | **Fixo** no nível mais externo — 1152×480 (seção 8.5); é o "topo" da árvore de Auto Layout dessa amostra, dimensionado pelo layout da seção Work, não hug nem fill | `316:423` |

**Leitura para a Fase 2:** padrão esperado de Auto Layout bem construído — elementos "atômicos" (botão de ícone, tag) usam **hug** nos dois eixos (equivalente a `width/height: fit-content` em CSS), enquanto elementos de layout (colunas de um card) usam **fill** (equivalente a `flex: 1 1 0%`). É reaproveitável na implementação Angular como comportamento de resize, mas **não é uma constraint de redimensionamento com limites**: não há `min-width`/`max-width`/`min-height`/`max-height` configurados em nenhum nó inspecionado (nem aqui, nem nos já cobertos na seção 8) — o Figma define só o modo (hug/fill/fixed) no tamanho atual, não até onde o componente pode crescer/encolher.

### 9.3 Prop de tamanho enumerável (Icon Button)

**Não é possível enumerar formalmente os valores aceitos pela prop `size` do Icon Button** (nem `state`/`themeMode`) além do que já estava registrado na seção 8.3. Confirmado nesta rodada de duas formas adicionais:

1. `get_design_context` numa segunda instância (`317:734`, Hero) devolve exatamente a mesma assinatura já vista no Header — `size?: "md"; state?: "Default"; themeMode?: "Light"` — porque essa tool reflete os valores da **instância específica**, não a lista completa de variantes do componente principal.
2. As tools que enumerariam a definição do componente principal (`get_context_for_code_connect`, `list_file_components_for_code_connect`) continuam bloqueadas pela mesma restrição de plano já registrada na seção 8.3 (exige seat Dev/Full em plano Organization/Enterprise). Confirmado também, via `get_metadata` no nível do arquivo, que o arquivo tem **uma única página** (`327:868`, nomeada "Thumbnail" — contém todos os frames Home/Light/Dark da seção 1): não existe página separada de "Components"/biblioteca onde variantes do Icon Button (`size="sm"`? `state="Hover"`?) apareçam como frames estáticos navegáveis manualmente. Não é limitação de esforço de busca; é estrutural, dado o plano e a organização do arquivo.

**Conclusão:** a prop `size` do Icon Button existe e é nomeada (não é freeform), mas o único valor confirmado continua sendo `"md"` — outros valores possíveis (se existirem) não são descobríveis com as ferramentas/permissões disponíveis. Mantém-se a orientação já registrada na seção 8.3: tratar `sm`/`lg` (se a Fase 2 quiser variantes de tamanho do botão de ícone) como decisão de implementação, não como extração do Figma.

### 9.4 Síntese — por que isso importa para a Fase 2

**Não existe regra formal de sizing** do tipo "escala nomeada" no Figma (nem variável, nem prop enumerável) — mesmo resultado já visto para espaçamento (seção 2). O único dado formal e reaproveitável encontrado foi o **modo de resize do Auto Layout** (9.2: hug vs. fill), consistente entre os componentes amostrados e diretamente mapeável para `width/height: fit-content` vs. `flex: 1` em CSS. Fora isso, os tamanhos de componente da seção 8 (36×36/44×44 do Icon Button, 1152×480 do card, 896px do item de timeline etc.) continuam sendo **valores fixos hardcoded no Figma**, sem sistema de sizing por trás. Para a Fase 2, isso significa que os componentes Angular precisarão definir sua própria lógica de resize responsivo entre os breakpoints já documentados (seção 2: mobile 375px / tablet ~768–1023px convencionado / desktop 1440px) — por exemplo, via `clamp()`, `min-width`/`max-width` ou breakpoints de mídia — já que o Figma não fornece constraints de redimensionamento reaproveitáveis nem uma escala de tamanho nomeada para servir de base.

## 10. Decisões de conteúdo real vs. Figma (Fase 2)

> O Figma é um template genérico (pessoa fictícia, projetos fictícios) — o conteúdo real do currículo do Johnny (hoje só em `src/pt/index.html`) não bate 1:1 com a estrutura/assets do Figma. Esta seção registra as decisões de como resolver essas lacunas ao construir as seções da Fase 2, para não reabrir a discussão a cada componente. **Não são dados extraídos do Figma** — são decisões de produto/conteúdo, confirmadas pelo Johnny.

- **Skills — ícones sem match**: dos skills reais (Spring Framework/Spring Boot, Node.js, Java SE 8, Servlet/JSP, OOP, MySQL, Oracle Database, SQL Server, HTML5, CSS3, RWD, JavaScript, jQuery, LESS, Grunt, Git, soft skills), só 3 tinham ícone já exportado do Figma com match direto: `icon-javascript`, `icon-nodejs`, `icon-git` (seção 6). Os demais não tinham ícone disponível (o Figma tem ícones de uma stack mais moderna — Next.js, Nest, Socket.io, Tailwind, Cypress, Storybook — que não são skills reais do Johnny) — decisão original: renderizar esses itens **texto-only**, sem inventar ou baixar ícone novo de outra fonte.
  - [x] **Decisão revertida (07/09/2026, a pedido do Johnny):** a decisão "texto-only, sem baixar de outra fonte" deixava a seção real muito diferente do grid all-icon do Figma (só 3 de 20 itens com ícone). Johnny indicou o [Devicon](https://github.com/devicons/devicon) (MIT) como fonte — 9 ícones adicionais baixados de lá (variante `original`, todas em `viewBox 0 0 128 128`) fecham o match de: Spring (`icon-spring.svg`), Java (`icon-java.svg`), MySQL (`icon-mysql.svg`), Oracle Database (`icon-oracle.svg`), Microsoft SQL Server (`icon-sqlserver.svg`), HTML5 (`icon-html5.svg`), CSS3 (`icon-css3.svg`), jQuery (`icon-jquery.svg`) e Grunt (`icon-grunt.svg`). **LESS não tem ícone "só símbolo" no Devicon** (só existe como `plain-wordmark`, a palavra "less" estilizada, não um ícone quadrado) — decisão: LESS foi **substituído por Sass/Scss** no conteúdo real da seção (o `icon-sass.svg`, seção 6, já existia — o template do Figma também tinha Sass/Scss no grid original, só não tinha match com o conteúdo real antes dessa troca). Ficam texto-only, ainda sem ícone disponível em lugar nenhum: **Servlet/JSP** (não é uma marca com logo próprio), **OOP** e **RWD** (conceitos, não tecnologias/marcas) e os 4 itens de "Pessoais" (traços de personalidade, fora do escopo de "ícone de tecnologia"). Ver seção 6 para a tabela desses 9 assets externos (não são exports do Figma) e o componente `angular-app/src/app/features/skills/skills.ts` para a lista final.
  - [x] **Layout também revisto (07/09/2026, a pedido do Johnny):** a implementação original agrupava os itens por categoria do currículo (Back-End/Banco de Dados/Front-End/Versionamento/Pessoais) com um título `<h3>` por grupo — uma adaptação de UI que o Figma não tem (seção 8.10 é um grid flat, sem categorias). Revertido para um grid único sem título de categoria, mais fiel à estrutura do Figma; a ordem dos itens na lista ainda segue o agrupamento original (itens da mesma área ficam juntos), só sem o cabeçalho visual.
  - [x] **Lista de skills atualizada (07/09/2026, editada diretamente pelo Johnny em `skills.ts`):** a lista de skills reais em si mudou — LESS, Oracle Database, Microsoft SQL Server, jQuery, Servlet/JSP, OOP, RWD e os 4 itens de "Pessoais" saíram; entraram TypeScript, PostgreSQL, MongoDB, DynamoDB, Angular(JS), GitHub, AWS e Azure. Resultado: as 18 skills da lista atual têm **todas** ícone (nenhum item texto-only) — TypeScript/PostgreSQL/MongoDB já tinham ícone do próprio template do Figma (seção 6, sem match antes dessa atualização); os outros 5 novos (DynamoDB, AngularJS, GitHub, AWS, Azure) vieram do Devicon (seção 6.1, junto com Spring/Java/HTML5/CSS3/Grunt da rodada anterior — Oracle/SQL Server/jQuery, sem uso depois da troca da lista, foram removidos do repo por ficarem órfãos). **Achado de acessibilidade, corrigido no mesmo dia:** `icon-github.svg` (Devicon, `#181616`, quase preto) ficava ilegível no dark mode do site (preto quase-puro sobre o fundo escuro do tema — falha WCAG AA de contraste, confirmado visualmente pelo Johnny e no navegador). Corrigido com um filtro CSS (`filter: invert(1)`) aplicado só nesse ícone e só quando `[data-theme='dark']`, via um novo campo `invertOnDark` em `SkillItem` (`skills.ts`) — preto vira branco só no tema escuro, sem asset adicional. **Achado técnico, não é sobre este componente especificamente:** a primeira tentativa de implementar esse filtro usou `[data-theme='dark'] .minha-classe` direto no `skills.scss` e simplesmente não funcionou (sem erro, só não aplicava) — porque esse é um `.scss` de componente (encapsulamento `Emulated` do Angular), e `data-theme` fica no `<html>`, fora da view do componente; a correção foi usar `:host-context([data-theme='dark']) .minha-classe`, o mecanismo do Angular pra esse caso exato. Registrado como regra nova em `docs/agent-rules/angular-components.md` (seção "5. Validar") pra qualquer componente futuro que precise reagir ao tema a partir de CSS escopado. **Correção adicional no mesmo dia:** `icon-mongodb.svg` (30×64, não quadrado) disparava o aviso `NG02952` do `NgOptimizedImage` por estar forçado em 64×64 — corrigido com `iconWidth: 30` (mesmo padrão já usado no `icon-nodejs.svg`, `iconWidth: 57`, que já existia antes desta tarefa). **Duas últimas mudanças, a pedido do Johnny:** (1) adicionado `icon-vscode.svg` (Devicon, `vscode-original.svg`) como 19ª skill; (2) ordem da lista embaralhada de propósito, intercalando categorias uma a uma (nenhum par de itens adjacentes é da mesma área) — fixa no código-fonte, não gerada em runtime (evita risco de mismatch de hydration no SSR/prerender do projeto).
- **Redes sociais — ícones sem match**: `icon-social-github` bate com o GitHub real. As redes reais do Johnny são GitHub, LinkedIn e WhatsApp — o Figma só tem ícones exportados de GitHub/Twitter/Figma (seção 6), nenhum de LinkedIn. Decisão: GitHub usa `icon-social-github`; LinkedIn fica **texto-only** até confirmação (ver pendência abaixo); WhatsApp não precisa de ícone social novo — cai na linha de telefone do Contact me, que já usa `icon-phone` (seção 6).
  - [x] **Pendência resolvida (06/09/2026):** checado via `search_design_system` (query "LinkedIn", arquivo inteiro — retornou vazio) e via `get_metadata` no frame completo `316:177` (Home / Desktop / Light, as 9 seções) — nenhum nó nomeado "linkedin" existe em nenhuma parte do canvas; os únicos ícones sociais do arquivo, em qualquer seção, são os 3 já catalogados na seção 6 (GitHub, Twitter/X, Figma). `get_context_for_code_connect` no componente `Icon` (para enumerar formalmente a prop `name` e revelar variantes não usadas) segue bloqueado pela mesma restrição de plano das seções 8.3/9.3, então a ausência é confirmada só pelo que é buscável sem Code Connect — mas não há evidência de ícone de LinkedIn "escondido" no arquivo. Decisão de texto-only para LinkedIn (acima) fica confirmada. Detalhe completo em 8.12.
- **Logos de empresa (Experience)**: a spec do item de timeline (seção 8.6) prevê um logo de empresa por item (placeholder fictício "logo-upwork" no Figma). Nenhuma empresa real do Johnny tem logo exportado do Figma, e não é apropriado usar/gerar marca de terceiros sem fonte oficial — decisão original: o slot de logo vira **texto** (nome da empresa) no componente Angular, não uma extração do Figma.
  - [x] **Revertido parcialmente (07/09/2026, a pedido do Johnny):** o Johnny forneceu os arquivos de logo reais (fonte oficial de cada empresa, não gerados) em `public/assets/images/` para Totvs, Santander, Capgemini, Vivo, Telefônica Educação Digital e Nielsen do Brasil — `experience.ts` agora renderiza esses logos via `NgOptimizedImage` (campo `logo` opcional em `ExperienceItem`, `width`/`height` no aspect ratio real de cada arquivo, tamanho final controlado por `max-width`/`max-height` em `.experience__logo`). RCS Sistemas continua sem logo disponível — mantido como texto (`.experience__company`), mesmo tratamento de antes.
- **Foto (Hero/About)**: as imagens já exportadas (`image-hero-avatar.png`, `image-about-photo.png`, seção 6) são de outra pessoa (placeholder do template) — decisão já registrada (seção 7): sem foto real por ora. Complemento de forma visual: usar um placeholder neutro (bloco com cor de token + iniciais, por exemplo) no lugar do `<img>`, reaproveitando só a proporção/posição do slot.
- **Imagem de projeto (Work)**: `image-project-thumbnail.png` (seção 6) é de um projeto fictício do template; os 2 projetos reais (Pitcher, ProFuturo) são produtos corporativos internos sem material de divulgação disponível — decisão: o lado da imagem do card vira um bloco neutro (cor de fundo + borda + radius + sombra já documentados em 8.5), sem `<img>` de thumbnail fictício.
- **Work — quantidade de cards**: Figma tem 3 cards de projeto (seção 8.5); currículo real só tem 2 projetos — decisão: usar 2 cards reais, grid/lista adaptada para 2 itens em vez de 3.
- **Testimonials**: sem equivalente real (currículo não tem seção de depoimentos) — decisão: manter a estrutura do Figma (seção 8.7) com 3 depoimentos fictícios, claramente marcados no componente como placeholder temporário.

## Log de evolução

- **06/09/2026** — Espaçamento (seção 2): verificado via `get_variable_defs` em 4 frames de seção (Hero `316:194`, About `316:229`, Skills `316:256`, Work `316:415`) que o Figma **não tem variáveis de espaçamento nomeadas** — só cor/tipografia/efeito são tokens formais. Extraído via `get_design_context` o padding/gap real das mesmas 4 seções: padding de seção consistente em **80px horizontal / 96px vertical** nas 4 amostradas, e uma tabela de gaps/paddings internos observados (4 a 96px). Substitui a nota genérica anterior ("múltiplos de 8px") por dados medidos — a hipótese de base 8 estrita não se confirmou (6px e 10px aparecem no arquivo).
- **06/09/2026** — Extração de assets (ícones): 24 ícones exportados do Figma via `download_assets` em lotes manuais (Skills, depois Hero/Contact me/Footer), salvos em `src/assets/icons/`. Ver seção 6 para a lista completa e a seção 7 para a pendência restante (ícone do Header).
- **06/09/2026** — Verificação de tipografia: conferido via `get_design_context` em Hero (`316:194`), About (`316:229`) e Experience (`316:357`) que a tipografia usa exclusivamente a família **Inter** (Google Font padrão), sem fonte customizada embutida. Nenhum arquivo de fonte precisa ser exportado — ver nota na seção 4.
- **06/09/2026** — Extração de assets (imagens): 3 imagens raster mapeadas em lotes por seção (Hero, About, Work, Testimonials) e baixadas via `download_assets` — avatar do Hero, foto do About e thumbnail de projeto do Work (reusado nos 3 cards). Salvas em `src/assets/images/`. Testimonials não tinha foto raster (avatar ali é ícone SVG genérico). Ver seção 6.
- **06/09/2026** — Espaçamento (seção 2), cobertura completa das 9 seções: extraído via `get_design_context` o padding/gap real das 5 seções que faltavam (Header `316:588`, Experience `316:357`, Testimonials `316:510`, Contact me `316:537`, Footer `316:579`), completando a amostra anterior (Hero, About, Skills, Work). Confirmado que o padrão **80px horizontal / 96px vertical** se mantém em Experience, Testimonials e Contact me (7 das 9 seções agora no mesmo padrão). Header e Footer confirmam a exceção esperada por terem altura fixa de 68px: padding vertical medido em **16px (Header)** e **24px (Footer)** — valores distintos entre si, ambos mantendo os 80px horizontais. Confirmado também, via `get_metadata` no frame `316:177`, que a margem entre as 9 seções é **0** (posições `y` batem exatamente, sem gap). Nenhum valor de pixel novo apareceu além dos já catalogados na amostra original — só novos usos dos mesmos valores (ver tabela "Espaçamento observado"). Seção 2 agora cobre as 9 seções do template; nenhuma pendência de padding de seção restante.
- **06/09/2026** — Artefato de tokens CSS: gerado `docs/design-tokens.css` (novo arquivo) traduzindo mecanicamente cor (seção 3, light/dark via `[data-theme="dark"]`), tipografia (seção 4, um grupo de variáveis `--font-<token>-*` por estilo) e sombra (seção 5) para CSS custom properties, mais uma escala de espaçamento derivada (`--space-4` … `--space-96`, nomeada pelo próprio valor em px) a partir da tabela "Espaçamento observado" da seção 2 — nenhum token formal de espaçamento existe no Figma, então essa escala é uma convenção documentada no cabeçalho do próprio arquivo, não um dado extraído. Durante a montagem dos tokens de tipografia, verificado via `get_design_context` (Hero `316:203`, estilo nomeado "Heading/H1/Bold - Desktop") que a unidade do letter-spacing da seção 4 é **porcentagem do tamanho da fonte**, não pixels (`-2` = `-2%`) — nota adicionada na seção 4 e valores convertidos para `em` no CSS. Referência ao artefato adicionada como nova subseção "Artefato de tokens CSS" logo após a seção 2.
- **06/09/2026** — Especificações de componentes individuais (nova seção 8): extraído via `get_design_context` (e `get_metadata` prévio para localizar node IDs) o Header (`316:588`), Footer (`316:579`), Icon Button (2 variantes de tamanho — 36×36 e 44×44 —, instâncias no Header/Hero/Work/Contact me), Tag (`325:214`, mesmo componente em 6 seções), os 3 cards de projeto do Work (`316:423`, `327:277`, `327:310`), os 3 itens de timeline da Experience (`316:365`, `316:378`, `316:405`) e os 3 cards de depoimento da Testimonials (`316:519`, `316:525`, `316:531`). Cada um documentado com dimensão, border-radius, cor (referenciando seção 3), tipografia (referenciando seção 4) e padding (referenciando seção 2). Duas limitações registradas explicitamente (não adivinhadas): (1) o comportamento sticky/scroll do Header não pôde ser confirmado — só uma borda transparente sugestiva, sem frame de estado "rolado"; (2) as variantes `Hover`/`Active` do componente Icon Button não puderam ser enumeradas (Code Connect indisponível no plano do arquivo; nenhum frame estático com esse `state` foi encontrado) — só a variante `Default` está documentada. Confirmado também que a timeline da Experience **não tem** conector visual (linha/marcador) — são cards empilhados com gap de 48px, verificado via `get_screenshot`. Pendência da seção 7 sobre especificações de componentes marcada como concluída, com a ressalva do Icon Button registrada no próprio item.
- **06/09/2026** — Sync de pendências (seção 7), sem extração nova de conteúdo visual: (1) a pendência "decidir dark mode/menu mobile" estava desatualizada — a decisão ("ambos entram no escopo") já constava em `docs/PLANO-MIGRACAO-ANGULAR.md` desde uma atualização anterior (seção "2. Decisões arquiteturais já tomadas" e "8. Log de evolução deste plano", 06/09/2026, "Johnny confirmou"); Johnny reconfirmou agora, e o item foi marcado como resolvido aqui, só referenciando o plano (nenhuma decisão nova tomada neste documento). (2) Reconfirmada, via `get_screenshot` nos grupos `328:4104` (Light) e `328:4105` (Dark) já documentados na seção 1, a ausência de qualquer frame/breakpoint de tablet no Figma — só as duas larguras já conhecidas (Desktop 1440 / Mobile iPhone 8 375) existem, em ambos os temas. Johnny decidiu que **tablet entra no escopo** mesmo assim; como não há dado extraível do Figma para essa faixa, documentada na seção 7 uma **convenção de engenharia proposta** (não uma extração): breakpoint ~768–1023px / desktop a partir de 1024px (padrão de mercado, ex. Tailwind `md`/`lg`), com o layout intermediário explicitamente marcado como algo a desenhar na Fase 2 (não copiável do Figma), a partir dos paddings/gaps da seção 2 — identificada de passagem uma lacuna: a seção 2 não tem um padding lateral de mobile medido para servir de segunda ponta dessa interpolação; não extraído nesta rodada para não estender o escopo desta atualização.
- **06/09/2026** — Padding de seção mobile (seção 2), resolvendo a lacuna registrada na entrada anterior: localizados via `get_metadata` no frame `327:417` (Home / Mobile (iPhone 8) / Light, seção 1) os node IDs mobile da mesma amostra usada no desktop — Hero (`327:419`), About (`327:442`), Skills (`327:468`) e Work (`327:638`). Extraído via `get_design_context` (Hero e Skills confirmados diretamente pela classe gerada `px-[16px] py-[64px]`; About e Work cross-validados por geometria via `get_metadata`, mesmo padrão `x=16`/`y=64` simétrico) o padding de seção no mobile: **16px horizontal / 64px vertical** nas 4 seções amostradas — nova tabela "Padding de seção — Mobile" adicionada logo após a tabela do desktop na seção 2. Com isso, as duas pontas da interpolação de tablet mencionadas na pendência da seção 7 (desktop 80px/96px, mobile 16px/64px) estão confirmadas; a nota da seção 7 foi atualizada para refletir isso, mantendo a ressalva de que o breakpoint e o layout de tablet em si continuam sendo convenção proposta, não extração (o Figma não tem frame nessa largura).
- **06/09/2026** — Assets sincronizados para dentro do projeto Angular: os 25 arquivos restantes de `src/assets/` (22 ícones + 3 imagens; `icon-footer.svg` já estava copiado desde a fatia 1) copiados para `angular-app/public/assets/`, sem filtro — inclui os 3 PNGs de placeholder do template, que as próximas tarefas de seção decidiram não usar como `<img>` final (ver nova seção 10).
- **06/09/2026** — Nova seção 10 "Decisões de conteúdo real vs. Figma": registradas as decisões de mapeamento de assets/conteúdo para a Fase 2 (skills sem ícone → texto-only, redes sociais sem ícone → GitHub com ícone/LinkedIn texto-only/WhatsApp reaproveita `icon-phone`, logos de empresa → texto, fotos Hero/About/Work → placeholder neutro sem `<img>` de template, Work com 2 cards em vez de 3, Testimonials como placeholder fictício marcado). Não são extrações novas do Figma — decisões de conteúdo confirmadas pelo Johnny, documentadas aqui para as próximas tarefas de implementação só consumirem, sem reabrir a discussão a cada componente.
- **06/09/2026** — Consumo pelo agente `angular-components` (primeira fatia da Fase 2 — tema + Icon Button + Tag + Header + Footer, ver `docs/PLANO-MIGRACAO-ANGULAR.md`, "Log de evolução"): nenhuma pendência nova identificada nas seções usadas (2-9); a pendência do ícone de tema/menu do Header (seções 6/8.3) segue em aberto, contornada na implementação com um placeholder SVG genérico comentado como tal. Observação (não bloqueante, não corrigida aqui — fora do escopo deste agente): o SVG já exportado de `icon-footer` (`src/assets/icons/icon-footer.svg`) carrega estrutura remanescente do frame de origem no Figma (retângulos de fundo de 1440×6498/1440×68 fora do `viewBox`, grupos aninhados "Light Mode"/"Home / Desktop / Light"/"Footer"/"Container"/"Row"), renderiza corretamente como ícone isolado (clipado pelo `viewBox`/`overflow` padrão do `<svg>`), mas fica maior/mais verboso do que precisaria; e usa `stroke` fixo (`#4B5563`, equivalente a `--color-gray-600` light) em vez de `currentColor`, então não acompanha automaticamente a troca de tema quando o dark mode for ligado — sinalizado aqui para o `designer` avaliar um re-export mais limpo quando for tratar os demais ícones/dark mode.
- **06/09/2026** — Consumo pelo agente `angular-components` (T6 do plano de implementação — componente Hero, `angular-app/src/app/features/hero/`), a partir da seção 8.8: duas colunas (`gap: 48px`), coluna esquerda com `Content` (H1 + bio) + `Actions` (link GitHub + LinkedIn), coluna direita com a composição em camadas no lugar da foto (retângulo `Background` deslocado 40px atrás + bloco frontal com borda `--color-gray-default`). Decisões tomadas nesta fatia (registradas aqui para rastreabilidade, não são extração nova do Figma): (1) o bloco `Location + Hire` da spec 8.8 foi **omitido**, não inventado — o currículo não tem dado real de localização/disponibilidade e a seção 10 não decide esse caso; sinalizado para o Johnny avaliar se vale inventar algo aqui numa fase futura. (2) GitHub usa o ícone real (`icon-social-github`, seção 6) dentro de um Icon Button que agora vira link (`<a>`, ver nota abaixo sobre a mudança no componente); LinkedIn (sem ícone no Figma, seção 10/8.12) virou link de texto simples ("LinkedIn") em vez de Icon Button, para não embutir um SVG de fora do Figma. (3) Bloco de foto usa `--color-gray-600` como cor do bloco neutro com iniciais "JS" — token escolhido por decisão de implementação (contraste ok nos dois temas), já que o Figma não define cor pra esse slot (lá é uma foto de verdade). **Achado técnico durante a implementação (não é dado de design):** o `Icon Button` (seção 8.3) precisou de um `href` opcional pra funcionar como link externo real (Hero/Contact me têm redes sociais, Work tem link de projeto) — colocar um `<button>` dentro de um `<a>`, ou vice-versa, seria HTML inválido, então o componente agora escolhe entre `<a>`/`<button>` via `@if`/`@else`. Isso expôs um bug do Angular 22.1.5: `<ng-content>` duplicado nos dois branches de um `@if`/`@else` faz a projeção de conteúdo falhar silenciosamente (elemento raiz renderiza vazio, sem erro no console) — confirmado com um teste isolado antes do fix. Contornado com `ngTemplateOutlet` (um único `<ng-content>` dentro de um `<ng-template>`, reprojetado em cada branch) — ver `angular-app/src/app/shared/components/icon-button/icon-button.html`. Vale re-testar esse padrão se o projeto atualizar de versão do Angular. Adicionado também o mixin `layout.section-padding` (`angular-app/src/styles/_layout.scss`) para reuso nas próximas seções de conteúdo (About, Skills etc.), análogo ao `bar-section-padding` já existente para Header/Footer.
- **06/09/2026** — Checagem de regras de sizing/resize de componentes (nova seção 9), por "desencargo" antes da Fase 2: (1) confirmado via `get_variable_defs` em 2 amostras novas (Header `316:588`, card do Work `316:423`) que o arquivo **não tem variáveis de tamanho nomeadas** (nada tipo `Size/*`/`Width/*`) — mesmo padrão já visto para espaçamento na seção 2. (2) Extraído via `get_design_context` o **modo de resize do Auto Layout** (hug contents / fill container / fixo) do Icon Button (`317:734`), Tag (`325:214`) e do card de projeto do Work (`316:423`): Icon Button e Tag usam hug contents nos dois eixos; as colunas internas do card usam fill container; a imagem do card (`Picture`) usa fill horizontal + altura fixa (384px); o card inteiro é fixo (1152×480). Nenhum `min-width`/`max-width`/`min-height`/`max-height` encontrado em nenhum nó. (3) Confirmado que **não é possível enumerar os valores da prop `size` do Icon Button** além de `"md"` (já registrado na seção 8.3) — `get_context_for_code_connect` e `list_file_components_for_code_connect` continuam bloqueados por permissão de plano (mesmo erro já visto), e `get_metadata` no nível do arquivo confirmou que há **uma única página** (`327:868`, "Thumbnail"), sem página de biblioteca/componentes separada para inspecionar variantes manualmente — limitação estrutural, não de esforço de busca. **Resultado geral:** nenhuma regra formal de sizing existe (mesmo veredito já dado ao espaçamento); o único dado reaproveitável é o modo hug/fill do Auto Layout (mapeável para `fit-content`/`flex: 1` em CSS) — os tamanhos em si continuam sendo valores fixos hardcoded, então a Fase 2 precisará definir a própria lógica de resize responsivo (`clamp()`, `min-width`/`max-width`) para os breakpoints da seção 2.
- **06/09/2026** — Specs finas das 6 partes que faltavam antes da geração dos componentes Angular (novas subseções 8.8–8.13), extraídas via `get_design_context`/`get_metadata` (Desktop/Light, exceto o item 6): **Hero** (`316:194`, 8.8) — composição 2 colunas (texto+ações à esquerda com `gap: 48px` entre Content/Location-Hire/Actions, foto em camadas à direita), tipografia por linha mapeada aos tokens da seção 4. **About** (`316:229`, 8.9) — checklist "quick bits" detalhada: 2 colunas × 2 itens, `gap: 10px` reaproveitado nos dois eixos (entre colunas e entre itens da coluna); foto sempre à esquerda, sem alternância. **Skills** (`316:256`, 8.10) — grid é 2 linhas flex `justify-between` de 8 itens (não CSS grid fixo), item = ícone 64px altura + label `Body1/Normal`, centralizado via `justify-center`; **achado central:** o grid não tem variante "texto-only" e o `justify-center` do item desalinha o label quando o ícone é omitido — resolve a dúvida da seção 10 sobre skills reais sem ícone, com 2 saídas de implementação propostas (não extraídas do Figma). **Experience — raiz** (`316:357`, 8.11) — heading é Tag + `Subtitle/Normal` (não H2, ao contrário do que o pedido presumia), e o mesmo `gap: 48px` do Container rege tanto heading→primeiro item quanto item→item (confirmado por geometria via `get_metadata`). **Contact me** (`316:537`, 8.12) — tudo empilhado numa única coluna central (heading → email → telefone → social), sem divisão em colunas lado a lado; aproveitado para checar a pendência de ícone de LinkedIn (ver abaixo). **Menu mobile/overlay** (`327:1337` Light / `327:3601` Dark, seção 8.13) — a instância real do overlay (`327:1772`/`327:4036`) isolada do resto da página mobile via `get_metadata` prévio (evitando reprocessar ~9400px de altura por seção): painel ancorado à direita (`left: 55px`, `width: 320px` de 375 totais) com scrim `blur(4px)` + cor a 10% de opacidade atrás, 3 blocos internos (Logo+fechar / 4 links About-Work-Testimonials-Contact / Switch Theme+Download CV), sombra `--shadow-2xl`; dark mode por troca de tokens de cor (mesmo padrão de inversão da seção 3) mais troca do ícone de tema (sol↔lua); sem frame de estado "fechado" nem gatilho de abertura (hambúrguer) dentro do frame do overlay — mesma limitação de prototype não confirmável já registrada para o Header sticky. **Checagem de LinkedIn** (pendência da seção 10, resolvida): `search_design_system` (query "LinkedIn", arquivo inteiro) retornou vazio e `get_metadata` no frame completo `316:177` (as 9 seções) não encontrou nenhum nó "linkedin" — nenhum ícone de LinkedIn existe no arquivo, usado ou não; decisão de texto-only (seção 10) confirmada, não havia asset a aproveitar.
- **06/09/2026** — Consumo pelo agente `angular-components` (T7/T8 do plano de implementação — componentes **About** e **Skills**, `angular-app/src/app/features/{about,skills}/`), a partir das seções 8.9 e 8.10: nenhuma pendência nova identificada nas seções consultadas (2-10) — as duas seções já tinham spec fina suficiente (8.9/8.10, extraídas numa rodada anterior) para gerar sem precisar do `designer`. Duas adaptações de conteúdo/UI registradas aqui para rastreabilidade (não são extrações novas do Figma, são decisões tomadas durante a implementação, coerentes com a seção 10): (1) About — os 4 "quick bits" da checklist (sem conteúdo real definido na seção 10 original) foram preenchidos com fatos reais e verificáveis contra `src/pt/index.html` (anos de experiência via `calculateExperienceYears`, já existente em `features/hero/hero.ts`; formação Uninove/Gestão da TI; inglês intermediário; estado civil — "casado e um filho", texto literal do legado). O candidato "localização" não tinha dado real disponível (confirmado via busca no repositório, nenhuma cidade documentada) e foi descartado sem inventar valor; "stack" também foi descartado por duplicar a seção Skills — o estado civil (fato real, sem duplicação) preencheu o 4º item no lugar dos dois. (2) Skills — o achado já documentado na seção 8.10 (grid flat sem variante texto-only, `justify-center` desalinha o label sem ícone) foi resolvido escolhendo a saída **(a)** já listada ali (slot de ícone com altura fixa reservada, 64px, sempre renderizado com ou sem `<img>` dentro), com a justificativa registrada no componente (`skills.scss`). Além disso, como o conteúdo real de Skills já vem categorizado no curriculo legado (Back-End/Banco de Dados/Front-End/Versionamento/Pessoais — src/pt/index.html, não a estrutura flat de 16 itens do Figma), o grid "2 linhas × 8 itens `justify-between`" da seção 8.10 foi adaptado para uma linha `flex-wrap` por categoria (contagem real variável, 5/3/7/1/4) — decisão de UI por convenção, não uma extração nova do Figma.
- **06/09/2026** — Consumo pelo agente `angular-components` (T9/T10 do plano de implementação — componentes **Experience** e **Work**, `angular-app/src/app/features/{experience,work}/`), a partir das seções 8.6/8.11 (Experience) e 8.5 (Work): nenhuma pendência nova identificada nas seções 2-9 — ambas já tinham spec fina suficiente para gerar sem precisar do `designer`. **Experience:** heading Tag + `Subtitle/Normal` (8.11, não H2) e `gap: 48px` uniforme entre heading/itens/itens (8.11); item card 896px/radius 12/shadow-md/padding 32px, sem conector visual (8.6, reconfirmado). Conteúdo real com as 6 entradas completas da "Experiência Profissional" de `src/pt/index.html` — Telefônica Brasil vira 2 itens de timeline separados (Analista SOC Ago2019-Mai2021 e Analista OSS Ago2017-Ago2019), já que são 2 cargos com período/atividades próprios dentro do mesmo bloco do legado. Slot de logo de empresa (102×28 no Figma) virou texto simples (decisão já registrada na seção 10) — tipografia escolhida por implementação (`Body2/Medium`, não extraída do Figma, já que o slot original era imagem, não texto) numa coluna de largura mínima fixa (160px, engenharia, evita nomes longos como "Telefônica Educação Digital" desalinharem o conteúdo entre itens). **Work:** card 100% coberto pela spec 8.5 (1152×480, radius 12, shadow-md no card/lg na imagem, padding 48px, título→descrição→tags→ação) — reaproveitado sem lacuna. **Achado novo, registrado aqui:** a seção root do Work (heading Tag+Subtitle centralizado, análogo a 8.9/8.10/8.11/8.12) não tinha uma subseção própria como as outras 4 seções de conteúdo com heading centralizado — resolvido por inferência do padrão idêntico repetido nas outras 4, não por extração formal do node `316:415` (documentado no componente `work.ts` para rastreabilidade, não é uma lacuna bloqueante igual às da seção 7). Imagem do card vira bloco neutro (seção 10, sem thumbnail real de projeto corporativo interno). Tags de tecnologia extraídas da descrição real de cada projeto (seção 10): Pitcher → MS-SQL Server/SAP/Salesforce; ProFuturo → HTML5/CSS3/JavaScript/jQuery/PIXI.js/Grunt/GitLab — nenhuma tecnologia inventada além do texto de `src/pt/index.html`. **Decisão sobre a ação do card (Icon Button "ver projeto"):** resolvida caso a caso com o dado real disponível, em vez de uma regra única para os 2 cards — Pitcher não tem nenhum link público no legado, ação **omitida** (não inventada, não aponta para um link genérico sem relação com o projeto); ProFuturo já tinha um link real no legado (`<a class="readmore" href="https://fundacaotelefonicavivo.org.br/profuturo/">Leia mais ...</a>`, página da Fundação Telefônica Vivo sobre o projeto), reaproveitado como a ação do card — mais fiel ao conteúdo real do que linkar os 2 cards para o GitHub/LinkedIn do Johnny (que não tem relação direta com "ver este projeto"). Ícone interno do Icon Button de ação: SVG genérico de "link externo" (mesma convenção já usada no ícone de tema/menu do Header — placeholder inline comentado como tal, sem spec/asset exportado do Figma para esse ícone específico). Ambos os componentes integrados em `pages/home/home.html` na ordem Experience → Work (após Skills, antes de Testimonials — ainda comentado). `npm run lint`, `npm run test` (54 testes, todos passando) e `ng build` (com prerender — "Prerendered 1 static route", `id="work"` e os textos "Pitcher"/"ProFuturo" confirmados no HTML gerado) validados sem erros. Nenhum `git commit` feito.
- **06/09/2026** — Correção de 8 ícones exportados (bug visual real, achado em revisão manual no navegador): além do `icon-footer.svg` já sinalizado (log de fatia 1), mais 7 ícones tinham o mesmo problema — `icon-copy`, `icon-mail`, `icon-phone`, `icon-pin`, `icon-social-figma`, `icon-social-github`, `icon-social-twitter` (todos os ícones de linha/UI; os ícones de marca/tech como `icon-javascript`/`icon-nodejs`/`icon-git` não têm esse problema, são exports limpos). Cada um carregava um `<rect fill="#F5F5F5"/>` de fundo (do próprio canvas do Figma) dentro do `viewBox`, renderizando uma caixa cinza-claro visível atrás do ícone (confirmado visualmente: o Icon Button do GitHub no Hero aparecia como um quadrado em vez de um ícone transparente). Removidos esse retângulo de fundo e os retângulos de frame (`1440×N`, fora do `viewBox` mas ainda no markup) dos 8 arquivos, em `src/assets/icons/` (fonte) e `angular-app/public/assets/icons/` (cópia usada pelo app) — mudança puramente mecânica (remoção de elementos redundantes), sem alterar o desenho do ícone em si; XML validado em todos os 8. **Limitação que permanece, documentada para as próximas seções (Contact me, T12; menu mobile, T13) não assumirem o contrário:** o `stroke`/`fill` desses ícones continua fixo (`#4B5563`, valor de `--color-gray-600` light) em vez de `currentColor` — e mesmo que fosse `currentColor`, os componentes atuais consomem esses SVGs via `<img ngSrc="...">` (`NgOptimizedImage`), e `currentColor` **não atravessa a fronteira de um `<img>`** (o SVG é carregado como recurso externo isolado, sem herdar `color` do documento hospedeiro) — então esses ícones não vão acompanhar a troca de tema automaticamente enquanto forem consumidos como `<img>`, independente da cor no arquivo. Cinza-médio funciona visualmente nos dois temas (contraste aceitável em fundo claro e escuro), então não é um bloqueio, mas é uma limitação a ter em mente: se um ícone precisar mudar de cor conforme o tema no futuro, a saída é inline SVG (`[innerHTML]` sanitizado) em vez de `<img>`, não só corrigir a cor no arquivo fonte.
- **06/09/2026** — Consumo pelo agente `angular-components` (T11/T12 do plano de implementação — componentes **Testimonials** e **Contact me**, `angular-app/src/app/features/{testimonials,contact-me}/`), a partir das seções 8.7 (Testimonials) e 8.12 (Contact me): nenhuma pendência nova identificada nas seções 2-9 — Contact me já tinha spec fina suficiente (8.12, extraída numa rodada anterior, incluindo a checagem de ícone de LinkedIn). **Testimonials**: confirmado que a seção 8.7 documenta só o card, não a raiz da seção (heading/fundo) — resolvido por inferência do mesmo padrão alternado já usado nas outras 6 seções de conteúdo com heading centralizado (Tag + Subtitle/Normal) e fundo alternado (Hero branco/About cinza/Skills branco/Experience cinza/Work branco/Testimonials cinza inferido/Contact me branco confirmado em 8.12/Footer cinza), mesma técnica de inferência já registrada no Work (T9/T10). **Achado novo, registrado aqui:** as medidas do ícone do avatar reportadas em 8.7 ("avatar circular 64×64 ... com um ícone de usuário genérico 40×40 dentro, padding: 20px") são internamente inconsistentes — um avatar de 64px com padding de 20px sobra só 24px de conteúdo, não 40px (40 + 2×20 = 80 ≠ 64); não é possível reconciliar os dois valores sem uma nova consulta ao Figma (fora do escopo desta tarefa, que não se conecta ao MCP). Resolvido na implementação com um ícone de 32×32 centralizado por flexbox dentro do círculo de 64×64 (o dado mais confiável, citado também na seção 6) — decisão de engenharia documentada no componente, não uma correção desta seção. Novo token de valor solto `--color-testimonial-avatar-bg` (`#9ca3af`) adicionado a `angular-app/src/styles/_tokens.scss`, mesmo critério já usado para outros valores sem token formal no Figma (ex. `--space-tag-padding-y`) — fixo nos dois temas, já que não é um token de cor `Gray/*` que inverte no dark mode. **Contact me**: layout 100% coberto pela spec 8.12 (coluna única central, sem lacuna nova). Conteúdo real (email/WhatsApp/GitHub com ícone, LinkedIn texto-only) já estava todo decidido nas seções 8.12/10 desta rodada anterior — nenhuma decisão de conteúdo nova aqui, só a implementação. Duas decisões de engenharia registradas: (1) email/telefone viram links reais (`mailto:`/`https://wa.me/...`), não só texto estático — não é extração da spec (que só documenta o conteúdo visual), mas consistente com GitHub/LinkedIn já serem links reais desde o Hero; (2) a ação de copiar (Icon Button 44×44, `icon-copy`) foi implementada de verdade via Clipboard API do browser (`navigator.clipboard.writeText`), guardada por `isPlatformBrowser` mesmo o método só rodando a partir de um `(click)` de template (que nunca dispara durante o prerender) — guarda defensiva, seguindo a mesma convenção já usada no `ThemeService` (ver `docs/ai-instructions.md`); feedback visual troca o ícone para um checkmark inline (sem asset exportado do Figma para esse estado — mesma convenção de placeholder já usada em outros ícones fora da spec) e o `aria-label` por ~2s. 17 testes novos (71 no total do projeto): Testimonials (host id, 3 nomes fictícios na ordem certa, nota de placeholder visível, ausência de `<img>` no avatar); Contact me (host id, layout empilhado — ordem real dos filhos diretos do container —, hrefs reais de email/WhatsApp/GitHub/LinkedIn, ação de copiar mockada incluindo reversão do feedback e rejeição silenciosa, e um bloco à parte confirmando via `PLATFORM_ID` sobrescrito para `'server'` que `navigator.clipboard` nunca é tocado no SSR). `npm run lint`, `npm run test` e `ng build` (com prerender — `id="testimonials"`/`id="contact"` e os textos reais confirmados no HTML gerado) validados sem erros. Nenhum `git commit` feito.
- **06/09/2026** — Consumo pelo agente `angular-components` (T13 do plano de implementação, `.claude/plans/splendid-stirring-flute.md` — **menu mobile dedicado**, `angular-app/src/app/layout/mobile-menu/`), a partir da seção 8.13: nenhuma pendência nova identificada nas seções 2-9 — a spec já cobria estrutura, cores/dark mode, sombra e o achado de que não há frame "fechado" nem gatilho de abertura mapeado (ambos resolvidos aqui como decisão de implementação, não extração). Confirmado o nome exato do token de sombra usado pela seção 8.13 ("Drop Shadow/2xl (dark)"): `--shadow-2xl-dark` (seção 5/`_tokens.scss`), sem precisar de token novo. Dois tokens novos adicionados a `angular-app/src/styles/` por não terem equivalente formal ainda: `--color-mobile-menu-scrim` (`_tokens.scss`, um valor por tema — 10% de opacidade sobre `--color-gray-900`/`--color-gray-dark-900`, mesma leitura já registrada em 8.13, "não é token formal da seção 3, aplicação ad-hoc de opacidade sobre o 900 já existente") e `--font-heading-h3-bold-mobile-*`/mixin `heading-h3-bold-mobile` (`_typography.scss`, 24/32/-2% — o próprio texto da seção 8.13 já registra que esse estilo do logo do painel "não estava na tabela da seção 4"). Achado de implementação, não uma correção da spec: a largura do painel (320px de 375 totais, ~85%) foi reproduzida como `width: min(85%, 320px)` em vez de um `left: 55px` fixo, pra funcionar de forma proporcional também no breakpoint de tablet (sem referência no Figma, seção 7/9.4) — com um teto maior (`min(60%, 400px)`) só no tablet, decisão de engenharia documentada no componente. Também documentado no componente: o ícone de tema do menu mobile troca sol↔lua (confirmado visualmente na spec 8.13), enquanto o ícone equivalente do Header (seção 8.1/8.3) segue estático desde a fatia de fundação — assimetria conhecida, não corrigida aqui por estar fora do escopo desta tarefa (T13 é só o menu mobile). `npm run lint`, `npm run test` (92 testes no total do projeto) e `ng build` (com prerender — `id="mobile-menu"` e `.header__menu-toggle` confirmados no HTML gerado) validados sem erros. Nenhum `git commit` feito.
- **07/09/2026** — Correção de um segundo bug de recorte em 4 ícones (achado em revisão manual com o Johnny, distinto do bug de fundo `#F5F5F5` já corrigido na entrada de 06/09/2026 acima): `icon-social-github`, `icon-social-twitter`, `icon-social-figma` e `icon-copy` tinham o `viewBox`/desenho escalados para o tamanho do **componente pai que os continha na extração original** (o `Icon Button`, 36×36 no caso dos 3 primeiros e 44×44 no `icon-copy`) em vez do tamanho do próprio ícone (24×24 e 32×32, respectivamente) — resultado: quando renderizado no `width`/`height` correto (24px/32px, já configurados certos no HTML desde o início), o desenho do ícone em si ficava visivelmente menor que o esperado, com um padding embutido extra somado ao padding do próprio Icon Button (6px, seção 8.3). `icon-pin`/`icon-mail`/`icon-phone` não tinham esse problema por nunca terem sido extraídos de dentro de uma instância de `Icon Button`. Causa raiz: a extração original desses 4 ícones veio de dentro das instâncias de uso (Hero/Contact me), que expõem só o bounding box do componente pai, não o do ícone isolado — o mesmo padrão já sinalizado como limitação na seção 6 ("o ícone real só aparece resolvido no screenshot, não no `get_metadata`"). Resolvido localizando a biblioteca canônica de ícones do arquivo, página "Styles & Components" (node `114:2`) > sub-área "Icons" (node `163:5`, ver nova nota na seção 6) — reexportados os 4 ícones dessa biblioteca via `download_assets` com `defaultFormat: "svg"` nos símbolos isolados corretos (`165:123`/`165:117`/`165:121`/`165:109`, todos Light), removendo manualmente do SVG retornado os artefatos de canvas do Figma (retângulo de fundo do frame, retângulos de anotação/redline do modo dev, grupos herdados da árvore de camadas) antes de salvar — mesma técnica de limpeza já aplicada na entrada anterior, aplicada aqui a um problema de escala/crop em vez de um elemento visível a mais. Arquivos atualizados só em `angular-app/public/assets/icons/` (o app lê daí; `src/assets/` não existe no projeto, ver correção de convenção na seção 6). Validado visualmente no navegador (`localhost:4200`, luz e escuro): ícone do GitHub no Hero e ícone de copiar no Contact me agora preenchem a caixa do Icon Button na mesma proporção que `icon-pin`/`icon-mail`/`icon-phone` já preenchiam. **Achado adicional sobre o processo, não sobre o design:** a biblioteca "Icons" (`163:5`) também expõe, prontos para uso futuro e não exportados nesta rodada por estarem fora do pedido, ícones que resolvem duas lacunas há muito abertas nesta seção — `icon-light-mode`/`icon-dark-mode` (tema do Header, pendência da seção 7) e `icon-menu`/`icon-close` (menu mobile) — e um `icon-external-link` sem equivalente hoje na tabela (útil para a ação "ver projeto" do Work, seção 8.5, hoje um placeholder inline genérico). **Ajuste no agente `designer`** (`docs/agent-rules/designer.md`, seção 3.1, e `.claude/agents/designer.md`): adicionada instrução para sempre procurar uma página/biblioteca canônica de ícones (`get_metadata` no arquivo inteiro, por nomes como "Styles & Components"/"Icons") antes de extrair um ícone de dentro de uma instância de uso, e adicionado `Bash(curl:*)` às tools do agente — ele só tinha `mcp__figma__*, Read, Write, Edit`, sem nenhuma forma de baixar de fato os bytes da URL retornada por `download_assets`/`get_screenshot` (essas tools devolvem uma URL de curta duração, não o conteúdo do arquivo); a ausência dessa capacidade é a explicação mais provável para os dois bugs de extração de ícone já registrados no log (fundo `#F5F5F5` e, agora, o crop errado) — sem forma de baixar o arquivo real, o SVG salvo provavelmente foi reconstruído/transcrito manualmente a partir da inspeção visual/código do `get_design_context`, um processo propenso a exatamente esse tipo de erro geométrico. Considerado e descartado dar `WebFetch` ao agente para isso: por passar o conteúdo por um modelo de resumo antes de devolver, arrisca alterar dígitos de atributos `d`/`points` do SVG.
- **07/09/2026** — Limitação de escopo do MCP do Figma, documentada em `docs/agent-rules/designer.md` (seção "3. Extrair do Figma"): confirmado em produção que `get_metadata` sem `nodeId` **não** lista todas as páginas do arquivo via API remota — lista só a página que está aberta no momento no Figma Desktop conectado. Com a página "Thumbnail" aberta, a tool só devolveu essa página, mesmo o arquivo tendo outras (ex. "Styles & Components", só localizada depois que o Johnny abriu essa página no Figma Desktop e confirmou). Regra nova: nunca concluir que uma página/frame "não existe" só por não aparecer num `get_metadata` sem `nodeId` — pedir pro usuário abrir a página certa no Figma Desktop, ou pedir um link com `node-id` direto ("Copy link to selection").
- **07/09/2026** — Revisão da seção Skills (`angular-app/src/app/features/skills/`), a pedido do Johnny, em duas partes: (1) **ícones** — 9 ícones novos baixados do Devicon (MIT) e um swap de conteúdo (LESS → Sass/Scss, ver seção 10 para o detalhe completo e a tabela de proveniência na seção 6.1); componente (`skills.ts`/`.html`) e testes (`skills.spec.ts`) atualizados para os 13 itens com ícone (era 3) e 7 texto-only (era 17). (2) **layout** — removido o agrupamento visual por categoria (título `<h3>` por grupo, ex. "Front-End"/"Back-End") a pedido do Johnny, que reparou que a versão com categorias tinha ficado visualmente distante do grid flat do Figma (seção 8.10); `groups: SkillGroup[]` (por categoria) trocado por `items: SkillItem[]` (lista única), mantendo a ordem original (itens da mesma área do currículo ainda ficam agrupados na lista, só sem o cabeçalho visual); `.skills__row` ganhou `justify-content: center` (decisão de implementação, não extraída — o Figma usa `space-between` numa grade fixa de 8 itens por linha, que não se aplica a uma lista única de 20 itens com quebra variável). Teste de títulos de categoria trocado por um teste confirmando a ausência deles. Validado visualmente no navegador (`localhost:4200`, luz e escuro) e via `npm run lint`/`npm run test`/`ng build` (com prerender) — todos sem erro novo (os 2 testes falhando em `about.spec.ts`/`work.spec.ts`, sobre o texto das Tags em inglês vs. português, são pré-existentes e não relacionados a esta tarefa, não corrigidos aqui). Nenhum `git commit` feito.
